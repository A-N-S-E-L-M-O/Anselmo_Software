# Selmo — Development documentation
*Updated session 35 · 2026-07-09 · v0.929 (beta)*

> **Read first:** the **Lessons learned** section near the end of this file, and **`selmo-bug-report.md`**.
> **Project language:** English only — see `selmo-manifesto.md`. Conversation with Fabio can be any language; every file artifact is English.

This is the engineering reference for Selmo: how it is built, the parameters that matter, the lessons that cost something to learn, and the changelog. The vision lives in the manifesto; the open defects live in the bug report. This file is organised by topic, with a condensed reverse-chronological changelog at the end.

---

## Client file map (chat.html + 11 JS files)

The client is `chat.html` (the **shell**: `<head>`, inline `<style>`, the whole body markup, the CDN `<script>` tags) plus **eleven first-party scripts**, all *classic* (non-module) scripts so every function stays global and the inline `onclick="fn()"` handlers keep working with zero rewiring. The front door (`selmo_https_proxy.py`) serves them straight from disk (`.js` mime added there, not blocked); bump the shared `?v=` query on every script tag when any of them changes, because `.js` is cacheable while only `.html`/`.json` are sent `no-store`.

**The split rule — definitions in modules, execution in boot (learned the hard way in v0.902).** A naive contiguous split of the old single file **failed**: classic scripts do NOT share function hoisting across files, only the global scope. So a top-level init statement in an early file that calls a function defined in a later file throws `ReferenceError` (e.g. `renderSessionList`/`syncThinkPrompt` not yet defined). The working architecture:

- **`selmo-i18n.js` … `selmo-sessions.js` (10 modules)** contain **only function declarations**, grouped by subsystem. Load order among them does not matter — function declarations become global as each script runs.
- **`selmo-boot.js`** contains **all top-level globals (`const`/`let`) and all init/event-wiring code, in original order**, and is loaded **LAST**. By the time it runs, every function from every module already exists, so there are no cross-file ordering errors.

So when adding code: a new **function** goes into the matching module below; new **startup code or a new global** goes into `selmo-boot.js` (keep it last). Each module starts with its own `'use strict';`.

| Script (load order) | Responsibility | Key functions |
|---|---|---|
| `selmo-i18n.js` | internationalisation strings and locale helpers | *(locale data, no public functions)* |
| `selmo-core.js` | power/cost gauges, odometer, poll | `pt` `ap` `makeGauge` `setGauge` `setOdo` `savePrice` `updCost` `resetWh` `resetWhSession` `resetToks` `poll` |
| `selmo-bridges.js` | service bridges, mic, web search | `checkWebBridge` `checkWhisperBridge` `toggleMic` `webSearch` `rewriteQuery` `loadProps` |
| `selmo-model.js` | profiles + sampling, message build, THINK control, model traces | `activeSP` `syncSampling` `setProfile` `updateProfileUI` `onCustomParam` `openProfile` `closeProfile` `apiMessages` `maxTok` `setThinkEnabled` `syncThinkPrompt` `thinkKwargs` `configureThink` `applyThinkMode` `modelInfoLine` `traceLine` `showStartupTrace` `announceModel` `markThinking` `toggleThink` |
| `selmo-docs.js` | file/image extraction, chunk-split | `extractDocx/Odt/Pdf/Spreadsheet/Pptx/Odp` `imgToJpegDataUrl` `addImageFiles` `loadFileAsImage` `handlePaste` `loadFile` `detectHeadings` `splitChunks` `clearFile` |
| `selmo-chat.js` | render/export, streaming, chunk pipeline, ensureLLM | `scrollBot` `handleKey` `autoResize` `addMsg` `imgThumbStrip` `newChat` `exportChat` `stamp` `dlBlob` `mdTableToTSV` `dlReplyMd` `dlReplyTsv` `copyToClipboard` `addDownloadBar` `stopMsg` `streamTokens` `makeThinkPanel` `splitDoc` `notReadyMsg` `processDoc` `processChunks` `askFileMode` `ensureLLM` |
| `selmo-send.js` | the send loop | `sendMsg` `isTypingFocus` |
| `selmo-media.js` | push-to-talk, VAD, image generation, TTS | `pttStart` `pttStop` `f32ToWav` `vadSetState` `vadResume` `vadAfterSpeak` `vadOnSpeech` `toggleVad` `toggleImgMenu` `pickImg` `genImage` `checkKokoroBridge` `toggleTts` `cleanForTts` `speakText` |
| `selmo-sessions.js` | sessions, header toggles, layout | `getSessions` `sessionTitle` `setImageMode` `toggleWeb` `storableMsg` `saveSession` `renderStored` `loadSession` `deleteSession` `renderSessionList` `mobToggle` `closeOverlays` `_micHttpsBanner` |
| `selmo-settings.js` | tray-in-browser panel (LLM + image model switch w/ editable flags, service controls) | `openSettings` `closeSettings` `loadSettingsModels` `selectLLM` `confirmSwitch` `loadImageModels` `selectImg` `confirmImage` `waitLLMReady` `unloadLLM` `reloadLLM` `exitSelmo` |
| **`selmo-boot.js`** (LAST) | all globals + all top-level init / event listeners (incl. nested `_onVVResize`) | *(no top-level function declarations)* |

**Editing rules carry over.** Never use the Edit tool on `chat.html` (BUG-META-01) — Python via bash only. The `.js` files are ordinary files, but after any change run `node --check <file>` and verify with the Read tool; the mount can still serve stale/NUL views (BUG-META-02), so cross-check against `git show HEAD:` when anything looks off. (The old monolithic `selmo-app.js` was removed in v0.902; its history is the fallback.)

**Tray-in-browser (`selmo-settings.js`).** The tray's interactive parts now live in the browser so the UI is fully usable from any remote device; the tray itself just manages the backend and shows the model in use. Opened by clicking the header `model:` label. Talks to the **control API on 8087** through the front door (`CTRL = /proxy/8087`).

Shipped: **LLM switch** with the exact `srv` flags + chunking size **editable at switch time** (the old tray "show + retype", now a textarea in the panel); **image-model switch** with editable params; **unload / reload / exit**. The control API (`_CtrlHandler` in `selmo_tray.py`) exposes: `GET /status`, `GET /models` (each `{name, srv, chunking_size}` from the ini match), `GET /image/models` (`{name, params}` + current), `POST /llm/unload`, `GET|POST /llm/reload`, `POST /llm/switch` (`{name, srv?, chunking_size?}` -> `_action_switch` with overrides), `POST /image/select` (`{name, params?}` -> writes `selmo-image-config.json`), `POST /control/exit`. After an LLM switch the panel polls `/props` until the model is up, then `location.reload()` so ctx/think/config refresh cleanly. The image bridge (`selmo_image.py`) now **re-reads its config on every `/generate`**, so an image-model switch takes effect without restarting the bridge.

**Startup change (v0.905).** The tray **no longer shows a model picker** and does not block on one: `main()` auto-loads the **last-used model** persisted in `selmo-state.json` (`_load_last_model` / `_save_last_model`; `_action_switch` writes it on every switch), launches the services + front door, and hands off to the tray. If no model is found it starts services-only and you load one from the browser. The tray menu lost its "Switch model" submenu (switching is browser-only); it keeps the status header (shows the model), open-browser, view-log, unload, reload, exit. The old `_gui_picker` is left defined but unused.

NB: 8087 binds `127.0.0.1` (loopback only). Remote clients reach it exclusively through the front door's `/proxy/8087` route, which is LAN-reachable — so switching/selecting/exiting is still reachable by anyone on the LAN/VPN, but the raw port is not directly exposed.

---

## Architecture at a glance

Selmo is one `llama.cpp` server, a small set of single-purpose Python bridges, and a single-page client (`chat.html` shell + 11 classic `.js` files — see "Client file map"). Everything runs locally; nothing leaves the machine except an explicit `/web` search.

| Port | Service | File |
|---|---|---|
| 8080 | **Front door** - serves `chat.html` + reverse-proxies every backend by port | `selmo_https_proxy.py` |
| 8081 | Web search bridge (SearXNG + DDG fallback + trafilatura) | `selmo_web.py` |
| 8082 | System-power monitor (system watts + Wh counter; GPU via NVML, CPU power estimated from load, LHM optional) | `selmo_gpu_monitor.py` |
| 8083 | Whisper STT | `selmo_whisper.py` |
| 8084 | Kokoro TTS | `selmo_tts.py` |
| 8085 | LibreHardwareMonitor remote web server (**optional** real CPU/GPU power) | external |
| 8086 | Image generation (stable-diffusion.cpp / Z-Image-Turbo) | `selmo_image.py` |
| 8087 | Tray control API (LLM load/unload coordination) | `selmo_tray.py` |
| 8089 | llama-server (LLM), now behind the front door (loopback only) | `bin/` + `selmo_tray.py` |
| 8443 | Front door over HTTPS (mobile-mic secure context) | `selmo_https_proxy.py` |
| 8888 | SearXNG (Podman container) | external |

`selmo_tray.py` launches llama-server (on private `127.0.0.1:8089`) and the bridges. The client only ever talks to the front door on 8080 (HTTP) or 8443 (HTTPS); the front door serves the page and routes `/proxy/<port>` to each backend, so the UI stays reachable even while the LLM is unloaded for image generation.

### Reference hardware

Intel i9-11900KF · 32 GB RAM · NVIDIA RTX 4070 Ti 12 GB · Windows 11.
Real GPU draw during inference 70–90 W, utilisation 40–99 %, temperature 50–60 °C.

### Software

| Component | Choice | License |
|---|---|---|
| Runtime | llama.cpp (CUDA) | MIT |
| Container engine | Podman Desktop (SearXNG) | Apache 2.0 |
| GPU monitor | pynvml | BSD-3 |
| TTS | Kokoro-ONNX | Apache 2.0 |
| Image gen | stable-diffusion.cpp + Z-Image-Turbo | MIT + Apache 2.0 |

---

## Setup & dependencies

Python 3.10+ (tested on 3.14), a single interpreter, no venv.

```
pip install flask faster-whisper pynvml trafilatura requests --break-system-packages
pip install kokoro-onnx soundfile langdetect psutil --break-system-packages --prefer-binary
```

| Package | Used by | Notes |
|---|---|---|
| flask | all bridges | lightweight web server |
| faster-whisper | `selmo_whisper.py` | STT, `small` model ~500 MB (auto-download) |
| pynvml / psutil | `selmo_gpu_monitor.py` | GPU watts/load/VRAM (NVIDIA) + system RAM |
| trafilatura / requests | `selmo_web.py` | web text extraction + HTTP |
| kokoro-onnx / soundfile / langdetect | `selmo_tts.py` | neural TTS + WAV + language detect |
| (stdlib only) | `selmo_image.py` | shells out to `sd-cli.exe` (stable-diffusion.cpp); no new pip dep |

**Manual downloads:** the LLM `.gguf` (any compatible model dropped in `models\`), an optional `*mmproj*.gguf` next to it for vision, the Whisper model (auto on first launch), and a Kokoro voice (`kokoro-onnx` releases). **External binaries:** llama.cpp CUDA build in `bin\`, Podman Desktop for the optional local SearXNG, and **LibreHardwareMonitor** (system-power source) installed by `setup-lhm.ps1` into `bin\LibreHardwareMonitor\`.

**Whole-system power (v0.816).** `selmo_gpu_monitor.py` reports a system-power estimate, not just GPU watts. It reads CPU package power and GPU power from LibreHardwareMonitor's remote web server (`http://127.0.0.1:8085/data.json`, vendor-agnostic — works for NVIDIA and AMD; the GPU figure no longer depends on NVML) and computes `wall ≈ (cpu + gpu + OTHER_DC) / PSU_EFF` (defaults `OTHER_DC=45 W` for board/RAM/drives/fans, `PSU_EFF=0.88` for an 80 PLUS Gold unit at partial load; both tunable against a wall meter). `setup-lhm.ps1` makes this reproducible with no manual GUI step: it downloads a pinned LHM release (v0.9.4) into `bin\LibreHardwareMonitor\`, writes `LibreHardwareMonitor.config` (web server on `:8085`, start minimized to tray), and registers a scheduled task that runs LHM elevated at logon (RAPL needs admin, so this skips the per-boot UAC prompt). `selmo_server.py` also launches LHM as a fallback when the task isn't used. The main dashboard gauge now shows **system watts** (CPU+GPU+losses, 0–500 W scale); the GPU%/temp/VRAM mini-gauges stay NVIDIA-only via NVML.

**Driver-free power & backend energy (v0.820).** LHM turned out to be a non-starter for a 5-minute install on an arbitrary machine: its WinRing0 driver needs admin and is quarantined by Microsoft Defender's vulnerable-driver blocklist, so CPU package power and core temperatures don't read on a locked-down box (the GPU still works — that's NVAPI/NVML, no driver). So **LHM is now optional**. When it isn't feeding real watts, `selmo_gpu_monitor.py` keeps GPU watts from NVML and **estimates CPU watts from utilisation** — linear idle→TDP, with laptop/desktop defaults chosen by battery presence (`psutil.sensors_battery()`): laptop `4→28 W`, desktop `20→125 W`, plus the `OTHER_DC`/`PSU_EFF` loss model; all four are tunable against a wall meter. `cpu_est:true` marks an estimate and the client prefixes it with `~`. CPU **temperature** is best-effort (LHM only, package→hottest-core) and hidden when unavailable. Net effect: the whole power panel needs only `pip install psutil pynvml`.

Energy is integrated in the monitor itself (`integrate_energy`, a 1 s tick of `sys_watts`) as the single source of truth: `wh_session` (resets each launch) and `wh_total` (persisted in `selmo-wh.json`, gitignored), both resettable via `GET /reset_session` and `/reset_total`. Every `chat.html` instance only displays these, so multiple tabs or devices can never double-count — the box draws one set of watts no matter how many UIs are open (the old client-side `localStorage` counter summed across instances). The dashboard regroups the hardware readouts under the gauge (watt split → VRAM/RAM → GPU/CPU load); the session-watt-hours odometer reads to 0.1 Wh.

---

## Launcher & server parameters

### INI-driven launcher (v0.807 → v0.811)

`Selmo.bat` carries no model logic. It reads `selmo-models.ini`, where each `[section]` is a case-insensitive substring matched against the model file name (**first match wins** — put specific names higher, e.g. `[LFM2.5]` above `[LFM2]`), with `[default]` as fallback.

Since **v0.811** each section holds one **`srv=`** line: the exact llama-server command, forwarded verbatim. At launch you pick the model, see its `srv` string, and press ENTER to keep it or paste a full replacement (a "show + retype" UX — `set /p` cannot pre-fill an editable line). `selmo_server.py` takes a single `--srv "<flags>"`, `shlex`-splits it, and prepends only the **four structural flags** the app cannot run without: `--model` (the menu pick), `--host 0.0.0.0`, `--port 8080`, `--path <base>`. Those four are stripped from `srv` if typed by mistake, so static serving and the LAN bind can never break. An `*mmproj*.gguf` in the model's own folder is auto-detected and appended **last** with `--batch-size/--ubatch-size 2048` (so our batch wins Gemma's non-causal ubatch assert — see Vision).

`chunk_ratio` / `chunk_maxtok` stay Selmo-only keys (written to `selmo-config.json` for the client, never sent to the server). **Caveat:** sampling flags (`--temp`/`--top-p`/`--top-k`) in `srv` set only the server default — `chat.html` sends its own per-request values and overrides them. Keep `--reasoning-format deepseek` (THINK panel), `--timeout 600` (phone uploads, BUG-IMG-02) and `--metrics` in the string; features depend on them.

Models live in folders LM-Studio-style: `Selmo.bat` scans `models\` recursively, each model remembers its folder, and the mmproj is matched within that same folder.

### NGL / CTX

`-ngl` and `--ctx` are no longer derived from file size. Defaults: `NGL=99` (all layers on GPU), `CTX=8192`.

**Why 8192:** `CTX=0` (a model's 32K+ training context) made Magistral allocate a ~20 GB KV cache on a 12 GB GPU (v0.714 OOM). 8192 is the safe ceiling for 22–24B models on the 4070 Ti.

**Why a prompt instead of size bands:** file size in MB does not predict speed — the GGUF's `block_count` (layer count) does. EuroLLM-22B has 54 layers, so a fixed `NGL=45` left 9 on the CPU → 6 t/s; Mistral-Small-24B has 40 layers, so the same NGL ran fully on GPU → 33 t/s, at near-identical disk size. Possible future step: read free VRAM from `nvidia-smi` + `block_count` from the GGUF and suggest values automatically.

**Thinking models and ctx (s9 decision):** never widen the server ctx for reasoning and never sacrifice `-ngl`. The workflow is chunk-based, so each piece is already small; room for reasoning tokens is reserved client-side in `chunk_pipeline.py` via `--thinking-buffer` (0 default, 800+ for reasoners). Discarded paths: lowering `-ngl` to fit ctx 16384 (Gemma collapsed 22 → 8 t/s) and a q8_0 KV cache (counterproductive with chunking).

### MoE offload (`--n-cpu-moe`)

To run a premium big MoE (e.g. Qwen3 30B/35B-A3B, ~3B active) on 12 GB: keep the dense backbone on the GPU, push experts to RAM. Lower N pushes more experts onto the GPU (faster) while VRAM holds. Wired through `selmo_server.py` and exposed in the `srv` string.

---

## Profiles & personalities

A profile bundles a **system prompt + sampling parameters + colour palette**, switched at runtime from the watermark/logo (click it to open the profile modal) — no server restart. As of **v0.815 there are three profiles** — Selmo (blue), Mizan (red), Custom (neutral) — each a `body` state with its own palette. The chosen profile persists in `localStorage` and is restored on reload.

### Selmo — blue, the assistant

Temperature 0.75, top-p 0.9. Warm, ironic, concise. Prompt trimmed to four lines (s14) so the model gets straight to the point:

```
You are Selmo, a local AI on the user's own hardware.
Direct, concise, and ironic. No preamble, no hype, no servility. When unsure, say so in a line. Never invent facts.
Reply in the user's language.
You don't browse; web results may appear in the conversation — use them when present. Never output [SEARCH:] tags.
```

*(v0.815 dropped the old `/web` wording from the prompt — the WEB toggle is the interface. The functional `/web` remnants in `chat.html`, the push-to-talk prefix and the `sendMsg` strip, are still there — see backlog.)*

### Mizan — red, the antagonist

Temperature 0.01, top-p 1.0. Deterministic, cold, no opinions, no first person. The narrative antagonist from *Dialoghi con la lavatrice* (see manifesto). Purpose: extraction, translation, code-checking — accuracy as the only criterion.

```
You are Mizan, an analysis system. Identify yourself as Mizan when asked.
Reply precisely and concisely. No opinions. No hesitation. No first person.
Extract data, translate, check code. Accuracy is the only criterion.

When web results are present in the conversation, use them. Never emit search tags.
```

*(v0.815: Mizan now names itself — previously it was an anonymous "analysis system" and refused to say who it was.)*

### Custom — neutral, the open profile

User-defined. A neutral grey **system palette** (no blue, no red) with the sampling parameters and system prompt exposed for direct editing, instead of inheriting a preset. Defaults: temp 0.7, top-p 0.95, top-k 40, with a generic helper system prompt. The values and prompt persist in `localStorage` (`scustomtemp`/`scustomtopp`/`scustomtopk`/`scustomsp`) and feed each request live.

### The three profiles (shipped v0.815)

| Profile | Palette | Personality | Parameters |
|---|---|---|---|
| **Selmo** | Blue (default) | warm, ironic assistant | preset: temp 0.75, top-p 0.9, top-k 40 |
| **Mizan** | Red (full palette) | cold analysis system, self-identifies | preset: temp 0.01, top-p 1.0, top-k 0 |
| **Custom** | Neutral grey (no blue/red) | user-defined | free: temp / top-p / top-k / system prompt, all editable |

**Implementation (chat.html, v0.815).** `currentProfile` is one of `selmo`/`mizan`/`custom`, persisted in `localStorage` and restored on load. `activeSP()` returns the active system prompt (`SP_SELMO` / `SP_MIZAN` / the editable `CUSTOM_SP`) and feeds `chatHistory[0]` via `syncThinkPrompt()`. `setProfile(name)` swaps `document.body` between no-class / `.mizan` / `.custom`, calls `syncSampling()` to set `currentTemp`/`currentTopP`/`currentTopK`, rewrites the watermark text, and refreshes the modal. Every chat request now sends `temperature`/`top_p`/`top_k` from those globals (the three `currentTemp` send-sites gained `top_p:currentTopP,top_k:currentTopK`). The modal — opened by clicking the logo — carries three badges (`.pm-selmo`/`.pm-mizan`/`.pm-custom`) and a parameters panel that is editable only under Custom and read-only otherwise.

**Palettes.** Each profile is a `body` state. `body.mizan` red-shifts the full token set via an **R↔B channel swap** that preserves luminance (`#001166` → `#661100`), with scoped overrides for every hardcoded blue surface (body gradient, header, nav/aside, gauges, session items, avatars, both bubbles, input area, think panel); accents go red/orange (`--cyan` → `#ff4d63`, `--yellow` → `#ff9a33`) rather than channel-swapped, so it reads as red. `body.custom` desaturates the same set to neutral greys. Both are scoped so Selmo's (unclassed) rules stay exactly as they were.

---

## Reasoning / THINK panel

The panel is fed only by what the server extracts — no client-side tag parsing (the old `indexOf('<think>')` scanner failed because tokens arrive split across SSE deltas).

- **`--reasoning-format deepseek`** in the `srv` string makes llama-server populate `reasoning_content`; the panel hooks that delta. Gemma streams reasoning out-of-band there too; receiving it forces `inTk=false` so Gemma's final answer can never land in the panel.
- **THINK button is template-driven** (read from `/props`, no hardcoded model list): the lowercased chat template contains `think` → button shown and ON by default; absent → hidden. If a model starts emitting reasoning mid-stream, the button re-activates itself (the model is in charge).
- **Two internal flags.** `REASON_FIRST`: the template opens `<think>` in the generation prompt (Olmo) → the parser starts inside the reasoning block. `INSTRUCTED = think-capable && !REASON_FIRST && !'<|think|>'`: only Magistral-style models get the manual `[THINK]` system instruction; native reasoners (Olmo `<think>`, Gemma `<|think|>`) never do, to avoid literal `[THINK]` leaking into the answer. `syncThinkPrompt()` is the single place that sets the thinking clause.
- **History keeps the panel.** Turns are saved as `[THINK]reasoning[/THINK]answer`; `renderStored()` re-splits on load so reasoning returns to the collapsible panel and only the answer stays in the bubble. `loadSession` also appends a centered model/ctx/reasoning trace at the top.

Classification verified against the GGUF templates: EuroLLM-9B (no `think` → hidden), Magistral-Small-2509 (`[THINK]` → instructed), Olmo-3-7B-Think (`<think>` → native), Gemma-4-12B-it (`<|think|>` → native).

---

## Documents, chunking, web, vision, voice

### Documents & chunking

The client extracts text from `.txt`, `.csv`, `.docx`/`.odt` (JSZip + namespace-aware DOMParser), `.xlsx`/`.xls`/`.ods` (SheetJS → CSV), `.pdf` (PDF.js, page by page), `.pptx`/`.odp` (JSZip + XML, slide by slide). Long documents are auto-chunked with a final summary.

**Two routing modes (from v0.705):** a file above ~50 % of ctx asks "Chunk it / Normal chat"; a light file goes to normal chat automatically with the document as context. This matters because `SP_TASK` ("output only the result, no commentary") is right for translation/extraction but wrong for analytical questions ("why don't the totals match") — there it makes the model jump to a confused conclusion. Analytical questions therefore go to normal chat (`SP_SELMO`), not the chunk pipeline.

**Verbatim-copy lesson (v0.714):** Magistral starts copying source text verbatim after summarising the first paragraphs of a long chunk — instruction-following drift as more source tokens accumulate. Reversing prompt/content order made it worse. Confirmed workaround: cap the output from the user prompt ("a 2–3 sentence abstract"); concise targets keep the model on task. `SP_TASK` was softened to "do not omit or add content *unless the prompt requires it*" so summarisation isn't treated as a copy task.

### Web search

Web access is **off by default** and explicit — nothing leaves localhost unless the user turns it on. The mechanism is the **WEB toggle button** in the header (`toggleWeb()`, `IS_WEB_ON`): when on (`WEB ●`) the message is sent as a web search; when off, it stays local chat. `selmo_web.py` uses local SearXNG in Podman with a DuckDuckGo fallback and trafilatura extraction. Results are injected as top-priority context, reusable in later messages, with `[1][2]` citations and a clickable source ledger (green dot = local SearXNG, yellow = public fallback = data leaving the machine). A separate path answers "what day/time is it" locally without a search.

**Leftover to clean up:** the old typed `/web <query>` command predates the toggle and still lingers in `chat.html` — the system-prompt text mentions `/web`, push-to-talk prepends `/web `, and `sendMsg` still strips a leading `/web`. The toggle is the real interface now; remove these remnants on the next `chat.html` pass.

### Vision (Gemma 4 / Magistral)

Selmo.bat auto-detects `*mmproj*.gguf` and adds `--mmproj`. The client sends one image **per page** at ~1280 px long-side as an OpenAI-compatible content array `[{image_url}, {text}]`; clickable thumbnails open full resolution. Analysis/OCR only — image *generation* is a separate pipeline (see Image generation below).

Gemma 4 has a **token budget per image** (70/140/280/560/**1120** for OCR) that fixes the interpreted resolution, so oversized or concatenated images are pointless — the model resizes anyway. The launcher uses the OCR budget in the mmproj branch: `--image-min-tokens 1120 --image-max-tokens 1120 --batch-size 2048 --ubatch-size 2048`. The batch/ubatch raise is essential: Gemma's vision encoder uses **non-causal** attention, so all image tokens must fit one ubatch — with the default 512, `GGML_ASSERT(n_ubatch >= n_tokens)` fires and the server dies (this was the real cause of BUG-IMG-01, not the message format). Output cap fixed in v0.809: `maxTok()` now reserves ~1300 tok/image + 800 headroom and gives the rest to the answer (up to 8000), instead of a flat 1200 that truncated vision answers.

### Image generation (text-to-image + img2img)

A pipeline separate from the chat LLM (a multimodal model is **not** needed): `selmo_image.py` (8086, Flask) shells out to **stable-diffusion.cpp** (`sd-cli.exe` in `bin\`) running **Z-Image-Turbo** — a 6B distilled model at ~8 steps, cfg 1.0. Three Apache-2.0 weight files live in `image\`: `z_image_turbo-Q6_K.gguf` (diffusion), `Qwen3-4B-Instruct-2507-Q4_K_M.gguf` (text encoder, passed as `--llm`), `ae.safetensors` (the FLUX autoencoder VAE). `/status` reports which of the binary + three files are missing; `/generate` (POST) returns a PNG. The tray (`selmo_tray.py`, `_start_all_services`) auto-starts the bridge; `selmo_https_proxy.py` routes `/proxy/8086` for the phone.

**Mode UI (v0.829).** The palette button (🎨) opens an embedded popup menu — Text-to-image, or Img2img Subtle/Medium/Strong — that opens upward and closes on outside-click. A native `<select>` was tried first (v0.828 UI) but replaced because three preset buttons wrapped badly on phone; the popup is one control. t2i uses the prompt only; **img2img reuses the loaded `+ IMG/OCR` image** as the init at its native aspect (prompt optional).

**img2img mechanics (v0.828).** `/generate` accepts a base64 `init_image` + `strength` (presets 0.4 / 0.55 / 0.7 — higher = further from the source). The bridge decodes it to a temp PNG and appends `--init-img <path> --strength <s>`, then deletes the temp. **CLI gotcha:** the old `-M img2img` mode was *removed* from sd.cpp — image gen is the default `img_gen` mode, and supplying an init image is what makes it img2img.

**VRAM swap (v0.830).** The 24B LLM (~10.5 GB) and the image model can't share 12 GB. By default the bridge now **unloads the LLM** before generating so the image model gets the whole GPU at full speed, then the LLM reloads lazily on the next chat. The coordination is a tray **control API on port 8087** (`selmo_tray.py`, stdlib `http.server`): `/llm/unload` stops llama-server and sets a `swapped_for_image` flag; `/llm/reload` relaunches the current model and clears it; `/status` reports both. The image bridge POSTs `/llm/unload` (then a 2 s CUDA-settle) before `sd-cli`, runs with **no** `--offload-to-cpu`, and never reloads — `chat.html`'s `ensureLLM()` does that before the next turn (an animated braille spinner so the reload never looks frozen; `/props` polled until the model answers). Because the tray is authoritative, a swap triggered from the phone heals on the desktop. **Fallback chain:** if the control API is unreachable or unload fails, the bridge falls back to `--offload-to-cpu` and coexists with the loaded LLM exactly as before; `--no-swap` forces that fallback, `--no-offload` forces GPU and skips coordination. While in image mode the WEB and THINK buttons grey out (they need the LLM); the palette button goes straight to text-to-image when no image is loaded. `--diffusion-fa` (flash-attn) always on. All-local: nothing phones home, the bridge only binds `127.0.0.1`/LAN; the control API binds `0.0.0.0` like the other bridges (LLM load/unload is reachable on the LAN — same trust model as ports 8080–8086).

### Voice

Full loop: microphone → Whisper → Selmo → Kokoro TTS → speaker. `selmo_whisper.py` (8083, faster-whisper) transcribes; `selmo_tts.py` (8084, Kokoro-ONNX, language auto-detect) speaks; Web Speech API is the always-available fallback that needs no server. Push-to-talk: hold Space or middle mouse → record → release → send; Ctrl+Space does PTT web search. **VAD hands-free (v0.800):** Silero VAD (`@ricky0123/vad-web`, ONNX in-browser) — tap once, speak, `onSpeechEnd` → WAV → Whisper → auto-send, reply forced to TTS. Anti-echo pauses the VAD during transcription/generation/TTS and resumes on the TTS completion callback. Library from CDN (cached after first use); for fully offline, serve the `.onnx`/`.wasm`/worklet locally.

### Mobile / HTTPS

Mobile browsers block `getUserMedia()` outside a secure context, and a LAN IP over plain HTTP is not one, so the mic is unavailable on the phone at `http://<ip>:8080` (it can chat, but never record). The front door (`selmo_https_proxy.py`) therefore also listens on HTTPS 8443 with a self-signed cert (`selmo.crt`/`selmo.key`, LAN IP in the SAN), giving the phone its secure context. Phone access: `https://192.168.x.x:8443/chat.html`, accept the cert warning once. Since v0.831 the cert **auto-regenerates when the LAN IP changes** (the IP is recorded in `selmo-cert-ip.txt`), so the old manual-delete step is gone. When `chat.html` is opened over insecure HTTP on a non-localhost host it shows a banner with a one-tap link to the 8443 page. See BUG-MIC-01 for the remaining cert-trust options (Firefox permanent exception / `about:config` / mkcert).

---

## Tested models

| Model | File | VRAM | ctx | t/s | Notes |
|---|---|---|---|---|---|
| Mistral Small 3.2 24B IQ3_M | `mistralai_Mistral-Small-3.2-24B-Instruct-2506-IQ3_M.gguf` | ~10.5 GB | 8192 | 32–33 | Production default · 40 layers → all on GPU |
| EuroLLM 22B Q3_K_M | `utter-project_EuroLLM-22B-Instruct-2512-Q3_K_M.gguf` | ~10.5 GB | 8192 | 6 | 54 layers → 9 stay on CPU at NGL=45 = slow; quality unremarkable |
| EuroLLM 9B Q4_K_M | `EuroLLM-9B-Instruct-2512.i1-Q4_K_M.gguf` | ~5.5 GB | 4096 | 70 | Pure ChatML, no reasoning; `n_ctx_train=4096` (capped automatically) |
| Gemma 4 12B Q6_K | `gemma-4-12b-it-Q6_K.gguf` | ~9–10 GB | 8192 | 22 | Multimodal, reasoning, Apache 2.0 |

**Ethical replacement candidate (s14):** **Magistral Small 2509** (Mistral, FR, Apache 2.0) — reasoning **+ vision**, same 40-layer architecture as Mistral-Small-24B (fast on 12 GB). GGUF `unsloth/Magistral-Small-2509-GGUF`: `Q3_K_S` (10.4 GB) or `UD-IQ3_XXS` (9.41 GB) + `mmproj-F16.gguf`. Reasoning via `[THINK]`. Fully-open-data text alternative: **OLMo 3 7B Think** (Ai2). **Qwen3 30B/35B-A3B** is the premium MoE option (experts in RAM via `--n-cpu-moe`); natively multimodal in the same unsloth repo, but llama.cpp vision for it is fresh — test before trusting.

Selection filter (from the manifesto): permissive license as a hard gate; EuroLLM = ethical default, Mistral = production default, Gemma = quality benchmark (measured, not distributed).

---

## Access

- **Desktop:** `http://127.0.0.1:8080/chat.html` (front door; localhost is a secure context, mic works)
- **LAN:** `http://192.168.x.x:8080/chat.html` (chat only - no mic, insecure context)
- **HTTPS (mobile mic):** `https://192.168.x.x:8443/chat.html` (front door over TLS - use this on the phone)
- **Remote:** VPN into the home router → the phone re-enters the LAN, no extra config

---

## Credits & licenses

All third-party components are open source; the permissive ones (MIT / Apache 2.0 / BSD / ISC / OFL) impose no copyleft on Selmo's code.

llama.cpp (MIT) · faster-whisper + CTranslate2 + Whisper weights (MIT) · Kokoro/kokoro-onnx (Apache 2.0) · onnxruntime(-web) (MIT) · @ricky0123/vad-web (ISC) · Silero VAD (MIT) · Flask (BSD-3) · requests (Apache 2.0) · trafilatura (Apache 2.0 ≥ v1.8) · langdetect (Apache 2.0) · pynvml (BSD-3) · soundfile (BSD-3) · Podman (Apache 2.0) · JSZip (MIT/GPLv3 dual) · SheetJS (Apache 2.0) · PDF.js (Apache 2.0) · marked (MIT) · Share Tech Mono (OFL) · stable-diffusion.cpp (MIT) · Z-Image-Turbo weights (Apache 2.0) · Qwen3-4B-Instruct text encoder (Apache 2.0) · FLUX VAE `ae.safetensors` (Apache 2.0).

**SearXNG (AGPL-3.0)** runs as a separate process in its own Podman container, reached over HTTP — Selmo neither bundles nor links it, so the copyleft does not reach Selmo's source. It stays optional and swappable (the web bridge falls back to DuckDuckGo). **Transparency note:** Silero, onnxruntime (Microsoft), Whisper (OpenAI), JSZip/SheetJS/PDF.js are open but not European — neutral with respect to the manifesto's "European AI" stance, like the other US-origin client libraries.

**Selmo's own license (s24).** Apache License 2.0 with the **Commons Clause** - source-available: anyone may use, study, modify and redistribute, but may not **Sell** the Software. Attribution to the Selmo project and the book *Dialoghi con la lavatrice* is **mandatory**, carried in the `NOTICE` file and propagated by Apache section 4(d). As sole copyright holder, Fabio keeps every right (commercial use, Microsoft Store) - the clause binds redistributors, not the author. See `LICENSE`, `NOTICE`, `README.md`.

---

## Lessons learned

**Editing `chat.html` — Python via bash only.** The Edit tool truncates this large file silently (BUG-META-01). After every change: extract the inline script and run `node --check`, verify with the **Read** tool (not bash `cat`/`wc`), confirm the file still ends with `</script></body></html>`. Then restart `llama-server` (it can serve the cached page) — anti-cache meta in the head + Ctrl+F5.

**The sandbox mount lies (BUG-META-02).** Tool/bash views of files on the mount can be NUL-corrupted or truncated (a stale cache served a 2025-line view of a 2361-line `chat.html` this session). The real files were intact — the **Read tool** and `git show HEAD:<file>` (object store) read true content; bash `cat`/`wc` did not. Never commit from the sandbox: a commit from a corrupted view poisons the repo. After any `.bat`/`.md` change check `python3 -c "print(open('f','rb').read().count(b'\x00'))"` returns 0; `.bat` files must be CRLF (an `^` continuation on LF breaks cmd). The Write tool is the NUL culprit; the Edit tool and Python writes stay clean.

**Git is the only safety net.** No `.bat` backups (deprecated). Only Fabio commits and bumps the version, from his own Windows shell — never the agent. On every positive feedback, prepare the change and hand Fabio a ready-to-paste commit that also bumps the version +0.001 (the `hbadge` in `chat.html` and this file's header). v1.0 is reserved for the real release. Costly s13 lesson: a working version that lived only in the working tree, never committed, was lost. **s17 reprise:** the docs described the Selmo/Mizan profile system as already built, but a preflight (`git log -S setProfile`, grep for `body.mizan`/`SP_MIZAN`) showed it had never reached the committed `chat.html` — only ancient v0.31 traces existed. The bash mount was clean (matched HEAD), so this was real lost/never-committed work, not a cache lie. Lesson: when docs and code disagree, trust `git` and the Read tool, and confirm the starting state before "completing" something — it may not exist yet.

**The language always follows the user.** Never hardcode a language in injected prompts — "reply in the same language as the user's message".

**Speed ≠ file size — layer count is what matters.** Read `block_count` from the GGUF; `-ngl` must cover it or layers spill to CPU (s14). Never sacrifice `-ngl` to widen ctx.

**KV cache & VRAM.** 22–24B at ctx 16384 overflows 11 GB free on the 4070 Ti; safe ctx 8192 for files > 9.5 GB. The "45 % rule" is about chunk-input vs the *running* context, not the launch ctx, and only holds for models that actually keep their nominal context (EuroLLM does not — kept at 8192).

**Reasoning belongs to the server.** No client-side `<think>` parsing (tokens split across deltas). `--reasoning-format` extracts it into `reasoning_content`; the panel hooks only that. `SP_TASK` silences reasoning ("output only the result"), which is wrong for analytical questions — those go to normal chat, not the chunk pipeline.

**Web search needs a query-rewrite step (s17, v0.817).** The engine query used to be the raw user message (`webQuery=txt.trim()`), so short follow-ups ("17 anni" → the *movie*), typos and chatty phrasing returned junk and the model wasted its turn apologising for useless results. Fix: a local RAG-style rewrite (`rewriteQuery`, cf. Rewrite-Retrieve-Read / HyDE) distils the recent turns + new message into a keyword query *before* searching; the sources footer then shows the query actually sent (`🔎 "…" ← original`) so it's auditable. Two traps cost a few rounds: (1) an in-prompt `/no_think` is **ignored** by reasoning models (Qwen, Gemma) — disable thinking via `chat_template_kwargs.enable_thinking:false` with a plain-call retry if the server rejects the field; (2) make extraction template-proof — demand a single `QUERY:` line and pull it from `content` + `reasoning_content` after stripping any `<think>`/`[THINK]` block, with a generous token budget as a net. On any failure (`!res.ok`, no `QUERY:` line, exception) the function returns the raw message, so behaviour never regresses.

**Vision.** One image per page, never a concatenated canvas (extreme aspect ratio, multi-MB base64). Gemma 4 has a per-image token budget (1120 for OCR) and a non-causal encoder that needs `--batch-size/--ubatch-size ≥ image tokens` (2048) or it crashes.

**Image generation (Z-Image-Turbo via stable-diffusion.cpp).** A pipeline separate from the vision LLM — a multimodal chat model is *not* needed for img2img; sd.cpp drives it. Two gotchas cost rounds: (1) the FLUX.1-schnell repo hosting the VAE is **gated** (needs HF login + terms), so pull `ae.safetensors` from the ungated `Comfy-Org/z_image_turbo` mirror (`split_files/vae/`) instead; (2) the sd.cpp CLI was refactored — `-M img2img` is **gone** (valid modes: `img_gen`, `vid_gen`, `convert`, `upscale`, `metadata`); image gen is the default `img_gen` mode and an `--init-img` is what makes it img2img. HF downloads: unauthenticated requests are throttled (set `HF_TOKEN`), and the Xet backend can stall — `HF_HUB_DISABLE_XET=1` falls back to plain resumable HTTPS.

**Never run index- or object-touching git from the sandbox (s21).** Running `git status` from the Linux mount let git rewrite `.git/index` against a stale/truncated view, poisoning a committed tree with an empty-filename entry (`fatal: empty filename in tree entry`); `git status`/`git reset` then both failed. Recovery: `git reset --mixed <last-good-commit>` on Windows + recommit the (intact) working tree. From the sandbox use **only** read-only git (`git log`, `git show`, `git ls-tree`) — never `status`/`add`/`reset`/`commit`. Reinforces the rule that all writing git stays on Fabio's Windows side.

**Server timeout.** `--timeout 600` (phone uploads) or `0` to disable; `should_stop` in the log is client disconnect, not a fault. **IQ3_M vs Q3_K_M:** same footprint, IQ3_M slightly better (importance quantization preserves critical weights).

**cmd `set /p`.** Each `set /p` must be on its own line — chaining with `&` does not run the second prompt.

**Rewriting history safely (filter-repo + Defender, s24).** Purging large blobs/secrets from git history is a once-before-publish job, and three traps each cost a round. (1) `git-filter-repo` pip-installs the *package* but its launcher script may not land on PATH, so `git filter-repo` returns `'filter-repo' is not a git command` and the purge is a **silent no-op** - the weights stay and the commit hashes are unchanged (that is the tell). Fix: add Python's `Scripts\` dir to PATH for the session, or run it as `python -m git_filter_repo`. (2) An **interrupted filter-repo run re-created the `empty filename in tree entry` corruption** (same as s21 / v0.829): a tree object written truncated, almost certainly Windows Defender locking `.git/objects` mid-write. The orphan bad tree plus the half-written dangling commits were unreachable, so `git gc --prune=now` removed them and `git fsck --full` came back clean - the real refs were intact (the weights still being present proved the refs had not moved). (3) **Exclude the repo folder from Defender real-time scanning before any history rewrite**, or it stalls mid-write and corrupts objects. Verify success with `git log --all -- <purged path>` (must be empty), `git rev-list --all --objects | findstr <secret>` (empty), and a shrunken `size-pack` from `git count-objects -vH`. Bundle the clean state afterwards.

---

## Pre-launch checklist (s31 — distribution & legal)

Blockers, in order, before shipping any bundle:

1. **Rebuild `dist\` from the current tree.** The existing bundle still carries the old brand, the Google Fonts link, `selmo-state.json`/`selmo-config.json`, `selmo-cert-ip.txt` (Fabio's LAN IP) and — critical — the **live `selmo.key` private key**. Exclude key/cert/state/IP files from the bundle: they regenerate on first run.
2. **Decide SEC-3** (front door unauthenticated on the LAN) — options 1–4 in `SECURITY-REVIEW.md`; decide before promoting phone access publicly.
3. **Bundled model licenses.** `dist\models\LFM2-8B-A1B` is LiquidAI's LFM Open License, *not* Apache — verify redistribution terms or swap it; ship each bundled model's license file next to the weights (NOTICE now points there). Mistral-7B-v0.3 and Olmo-3 are Apache 2.0: include their license texts too.
4. **Name/domain lock-in.** English book title finalized as *The Washing Machine Dialogues* (a noun-adjunct — not a genitive/apposition — in the "The … Dialogues" pattern, cf. *The Pentagon Papers*; shorter and more titular than the literal *Dialogues with the Washing Machine*, which can survive as a subtitle). The English edition ships under it and the domain matches. Register `thewashingmachinedialogues.eu` (+ `.com` if free; mind the spelling — 26 letters). **TMview clearance (s32):** the only exact "ANSELMO" is a Chinese CNIPA mark, Nice class 9 — territorial, no EU bearing — and ~200 "anselmo" variants exist (a common Italian surname), so freedom-to-operate holds in the EU for a free, non-commercial, source-available release; no IP clause added to `TERMS.md` (section 11 already allows per-version updates if scale ever changes). In public material A.N.S.E.L.M.O never appears alone: always co-branded with the domain / book title (naked "anselmo" searches belong to others).
5. **Legal review of `TERMS.md`** by a lawyer, especially the consumer-venue clause (Milan + art. 66-bis carve-out).
6. **English backronym locked** with the book translation: "Algorithm for Neural Synthesis with Emotional-Linguistic Memory Optimization. Selmo, to friends." — use this string everywhere.
7. **Shortcut & launcher cosmetics**: recreate `SelmoAI.lnk` with the new name; check `Selmo.bat`/tray console banners once more after a full run.
8. **Optional hardening**: self-host the five cdnjs/jsdelivr libraries in a `vendor\` folder (fonts already local) — makes the page fully offline and the privacy claim literally true on first load too.
9. **Marketing copy rule**: the license is **source-available** (Apache 2.0 + Commons Clause), never "open source" — see the positioning addendum.

---

## Nice-to-have / future work

**tokens/Wh efficiency log** *(arxiv publication candidate)*
Per-session append to a simple CSV or JSON: model name, quantization, average t/s, average system watts (GPU+CPU, already available from `selmo_gpu_monitor.py`), computed tokens/Wh. Data sources are all live: `/metrics` on llama-server for token counts, port 8082 for power. No UI needed — background logging only. Motivation: provides empirical MoE-vs-dense energy efficiency data on consumer hardware, directly in the spirit of the project's GDPR-by-design / local-first ethos. Magistral (dense 24B) vs Qwen3 (MoE 3B active / 30B total) on the same RTX 4070 Ti is a clean controlled experiment worth publishing.

**RAG / persistent memory** *(community contribution candidate)*
Index a local folder of documents (PDF, DOCX, TXT) into a persistent vector store; inject relevant chunks into context before each reply. Enables cross-session memory and corpus-level queries. Implementation: a local embedding model (`nomic-embed-text` or `all-MiniLM`) + `faiss` or `chromadb` for vector search — no external services. Out of scope for the core project (Selmo targets single-document, single-session workflows) but a natural extension for the open codebase. Not a priority until the base project stabilises.

**Parallel chunking — compare two versions of the same text** *(community contribution candidate · validation needed before it ships)*
Validate/consolidate two (or three) renderings of the same document: two translations against a reference, two contract drafts, an old vs new policy. The model adjudicates passage by passage ("A's phrasing here, B fixed a mistranslation there") and emits one complete consolidated artifact plus a sidecar of per-passage notes. This is the lawyer/editor use case, and it is **exhaustive, not RAG**: every passage must be covered, nothing retrieved-and-dropped — so it extends `chunk_pipeline.py` (reuse `extract`, `build_chunks`, `prove_coverage`, `auto_chunk_size`, `call_model`), it does not use the vector store. Design: **anchor one document** (the reference if present, else A) and chunk it with the existing provable coverage; for each anchor chunk, **align** the matching span in the other document(s). Alignment is the hard, fragile core and comes in three tiers — (1) **structural**, cut both at shared anchors (headings, clause numbers) and pair by label; cheap and exact when structure survives; (2) **embedding**, match paragraphs by cosine with a monotonic pointer for prose that lost its structure (this is the one place the RAG embedder is reusable — for alignment, not retrieval); (3) **proportional**, map by character ratio snapped to a sentence break via `_find_break` — a no-dependency floor that drifts on heavily rewritten text. Budget: A+B(+ref) share one context, so divide `available_tokens` by the number of versions. Adjudication prompt returns a `<<<MERGED>>>` / `<<<NOTE>>>` split so merged text stitches and notes accumulate. **Completeness guard:** `prove_coverage` already guarantees the anchor is fully covered; add a per-chunk output/input length-ratio check that flags any passage that collapsed (silent summarization) for a human look. **Why parked (s27):** misalignment fails silently — it confidently consolidates two passages that aren't the same text, catchable only by re-reading the whole book, which defeats the point. Bitext alignment is a real research area; this wants community-validated alignment before it's trustworthy. Lands as `selmo_parallel.py`, a sibling that imports from `chunk_pipeline.py` so the base pipeline is untouched.

**`no_think` as ini flag** *(general, not model-specific)*
Add `no_think=true` as an optional key in `selmo-models.ini`. `selmo_server.py` writes it to `selmo-config.json`; `chat.html` reads it and prepends `/no_think` to every user message. Works for any future model that supports the token, without hardcoding model names.

**Linux / macOS port** *(community-supported, post-distribution — not on the core path)*
Selmo's architecture is already LAN-served and OS-neutral at the client: `chat.html` + the 9 `.js` files run unchanged on any browser, and the Flask bridges (`selmo_web.py`, `selmo_whisper.py`, `selmo_tts.py`, `selmo_https_proxy.py`, `chunk_pipeline.py`) are mostly stdlib + portable pip deps. llama.cpp builds natively on Linux/macOS (CPU, plus Metal on Apple Silicon and CUDA/Vulkan on Linux). So a port is feasible, but it is **parked deliberately** until after distribution and left to community contributors — it is not worth pulling onto the core Windows roadmap. The work that a port actually requires, and the reason it is non-trivial:
- **`Selmo.bat` → a shell launcher** (`Selmo.sh`). Straightforward.
- **`selmo_tray.py` is the heavy item.** It is deeply Windows-specific: pystray, Windows Job Objects (`KILL_ON_JOB_CLOSE`), `pythonw.exe`, `CREATE_NO_WINDOW`. The 8087 control API logic is portable and worth keeping; the tray GUI + process supervision need a rewrite. Cleanest path: run headless under systemd (Linux) / launchd (macOS) and keep only the control API, dropping the tray icon.
- **Power monitoring is NVIDIA/Windows-bound.** `pynvml` and LibreHardwareMonitor do not exist off NVIDIA+Windows; on Linux/mac the watt/temp readouts come from a different source (`/sys/class/thermal`, `powermetrics` on mac, or a fixed estimate) or get hidden.
- **Image generation** ships an `sd-cli.exe` Windows binary; stable-diffusion.cpp compiles cross-platform but the GPU backend differs (Metal/Vulkan vs CUDA), so the image pipeline needs per-platform binaries + flag tuning.
- **A Raspberry Pi 8 GB was evaluated and rejected (s29).** It ports as software but has no usable inference GPU and only 8 GB shared RAM, so it is capped at 1.5–4B models at a few t/s — image gen impractical, the 24B production model impossible. The experience would undersell the project, so the Pi is explicitly **not** a target. Apple Silicon (Metal, unified memory) is the far stronger non-Windows host and the natural first port if a contributor takes it on.

**Split `chat.html` into per-subsystem files** *(DONE — v0.900 extracted the inline script to `selmo-app.js`; v0.902 subdivided it into 8 function modules + `selmo-boot.js`, and `selmo-app.js` was removed. See "Client file map" for the final layout and the definitions-in-modules / execution-in-boot rule. Historical plan kept below for context.)*
A single ~2,765-line, ~139 KB `chat.html` is the worst case for the mount/Edit-tool bugs (BUG-META-01/02): the Edit tool truncates it, so every change goes through Python-in-bash and re-reads/re-writes the whole file across the flaky bridge. Runtime cost of the single file is nil (Firefox handles it easily) — this is purely an editing-reliability win, not a performance one. Plan: extract the inline `<script>` into ~6–7 plain `.js` files loaded with classic `<script src>` tags (NOT ES modules, NO bundler), served statically by llama-server from the project root (works over the 8443 proxy too). Classic scripts keep every function global, so the inline `onclick="fn()"` handlers keep working with zero behavioural change; ES modules would force rewiring every event handler. Suggested split mirrors the subsystems: `selmo-core.js` (globals, config, `addMsg`, sessions), `selmo-chat.js` (`sendMsg`, streaming, THINK, `maxTok`), `selmo-profiles.js`, `selmo-docs.js` (files, chunking, export), `selmo-web.js` (toggle, `rewriteQuery`), `selmo-voice.js` (Whisper/Kokoro/VAD), `selmo-vision.js` + `selmo-imagegen.js`. Leave the `<style>` block inline (not the pain point; optional later cleanup). Cache-bust with versioned URLs (`selmo-chat.js?v=0.x`). Safe method (the extraction is itself the riskiest edit on this file): assemble the files in a scratch dir, `node --check` each, diff the concatenation against the original script to prove the JS is byte-identical apart from file boundaries, write across in one pass, verify with the Read tool. Land it as its own committed checkpoint BEFORE stacking new features.

---

## Changelog (condensed, reverse chronological)

- **landing site go-live** (2026-07-09; docs/webpages only - no software release change, stays v0.928 beta, chat.html badge unchanged). The public site under `docs/` is finalized and promoted. (1) **Real landing promoted**: the two-door content of `index-go-live.html` now lives in `index.html`, replacing the v0.922 coming-soon placeholder (which pulled a 6.9 MB cover); `index-go-live.html` removed. (2) **Live book links** wired in `site.js`: Amazon `amazon.it/dp/B0H7K87M2Y` and Kobo `kobo.com/ww/en/the-washing-machine-dialogues`, tracking params stripped. On `book.html` Kobo is now the primary CTA and Amazon the ghost, the cover (`assets/cover.png`) sits above the pitch, and the Italian-edition note was removed. (3) **Phone overlay** on `software.html`: a CSS device frame (rounded bezel, punch-hole camera, side buttons) floats over the interface window showing `assets/anselmo-phone.jpg`, stacking to a centered handset at <=720px. (4) **i18n aligned** across all four locales (en/es/zh/ar): the orphaned `book.it.note` dropped, a localized `phone.ph` added to match `fw.ph`; every markup key is now defined in every locale (only `hero.sub` stays defined-but-unused, by design). (5) **Polish**: entry cards centered in a 720px block, equal height with the `Enter` CTA pinned to a shared baseline and a two-line title reserve so the descriptions align; a non-breaking space binds "your PC?" so it no longer orphans onto its own line. No app-code change. Touched: docs/index.html, docs/site.js, docs/book.html, docs/style.css, docs/software.html, docs/assets/anselmo-phone.jpg (new), docs/index-go-live.html (removed), docs/selmo-dev.md.
- **v0.929** - QR code in the phone-access popup + quickstart/MODELS doc de-stale. (1) **Scannable QR of the phone URL.** The 📱 popup (`_openPhonePopup`, selmo-sessions.js) now shows a QR of the same `https://<ip>:8443/chat.html` address above the typed link, so a phone can scan instead of type (the address text stays as a labelled fallback). A self-contained byte-mode QR generator (`selmoQrMatrix` + `selmoRenderQr`, after Project Nayuki's algorithm, MIT) is **inlined in selmo-sessions.js as pure function declarations** - no new chat.html `<script>`, no CDN/external dependency, fully offline - drawn to a `<canvas>` with a 4-module quiet zone and wrapped in try/catch so any failure falls back to the address text only. Correctness proven by rendering the output and decoding it back with OpenCV's reader across the real IPv4 URLs (version 3, all 8 mask patterns pass); versions 1-7 verified clean. (2) **Docs corrected** (no app-code change): QUICKSTART.md + docs/quickstart.html no longer claim first launch "downloads the right engine for that PC (the NVIDIA build...)" - now the universal **Vulkan** engine that runs on any GPU or on CPU, with CUDA as the opt-in `Install-CUDA.cmd` add-on (it does not auto-select). The website quickstart was **missing the Install-CUDA.cmd add-on entry entirely** (said "Three", listed three) - added it and corrected the count to "Four"; QUICKSTART.md said "Three" while listing four - also "Four". docs/MODELS.md "The shipped `[Qwen3]` section" reworded so it is clear the tuned ini **flags** ship but the **model is not bundled** (you download it). MODELS.md path pointers aligned to `docs\MODELS.md` in both (was root `MODELS.md` / `installer\MODELS.md`). Build note: BUG-META-02 again - the bash mount served stale/truncated views of selmo-sessions.js, so `node --check` on the mount falsely failed; the inlined encoder was extracted and syntax+decode-verified in isolation, and every edit confirmed with the Read tool. selmo-sessions.js (`?v` -> 0.929) + chat.html (badge -> v0.929) + QUICKSTART.md + docs/quickstart.html + docs/MODELS.md; client-only for the QR (Ctrl+F5, no tray/bridge restart). Follow-up (docs-only, badge stays v0.929): docs/quickstart.html step 3 rewritten to describe the **automatic** Desktop/Start-menu icon created on first launch (then Pin to taskbar; `Create-Selmo-Shortcut.cmd` to restore it) - it had kept the stale manual "right-click -> Send to -> Desktop" instruction that QUICKSTART.md already dropped. `installer/build-bundle.ps1` now ships MODELS.md under the bundle's `docs\` folder (was the bundle root) so the `docs\MODELS.md` link in QUICKSTART step 2 resolves in the unzipped tree.
- **v0.928** - **BETA milestone: a clean from-scratch install now works end to end** (engine + image + CUDA), validated by rebuilding the production environment straight from the bundle. Backend/installer + image pipeline; the client badge is bumped to v0.928 with everything else so the whole release reads one version (no chat.html/js logic change beyond the version string - kept aligned on purpose to avoid version drift). (1) **Desktop / Start-menu Selmo icon.** New `installer/make_shortcut.py` creates `Selmo.lnk` (icon `selmo.ico`) targeting `pythonw.exe installer\boot.py` - a real .exe target, so it is pinnable to the taskbar (a .cmd is not); `boot.py` creates it once on first launch (marker-guarded, non-fatal) and `Create-Selmo-Shortcut.cmd` re-creates it. QUICKSTART now tells users to launch from the icon. (2) **CUDA backend add-on** (`installer/addon_cuda.py` + Install-CUDA.cmd / Uninstall-CUDA.cmd): NVIDIA-only, swaps the base Vulkan llama.cpp engine at the `bin\` root for the CUDA build + cudart libs, leaving the isolated `bin\sd\` (image) and `bin\LibreHardwareMonitor\` untouched; reversible via `revert`. Two bugs from the first cut fixed: the `bin-win-cuda` asset regex also matched the `cudart-...` zip (a DLL-only zip with no llama-server.exe), and it cleaned `bin\` BEFORE verifying the new engine - now it STAGES to a temp dir and verifies `llama-server.exe` before swapping, and prompts to close Selmo first (Windows locks the DLLs while it runs). `downloads.json` cuda_zip anchored to `^llama-...`; `first_run.py` docstring corrected (base = Vulkan for all GPUs and CPU; CUDA is the opt-in add-on). (3) **Image add-on: the Z-Image download always failed.** `min_bytes` was 5.5 GB but the real `z_image_turbo-Q6_K.gguf` is 5.26 GB, so the completed file was rejected as "too small" and never renamed off `.part`; all three weight sizes set to their exact bytes (verified against Hugging Face), and `download()` now finalizes a `.part` that already meets the size instead of resuming past EOF (which the server 416s). (4) **VAE out-of-memory on Vulkan fixed**: `selmo_image.py` now passes `--vae-tiling`, so VAE decode no longer allocates one ~8.5 GB buffer that overflowed 12 GB once the diffusion model + Qwen encoder stayed resident after the LLM swap - the image path runs on Vulkan even on a CUDA box, so this matters there too. (5) **Image model is now OFFERED and skippable** in `Install-Image.cmd` (a `[Y/n]` prompt, exactly like the first-run LLM), so a user can install just the engine and supply their own image model. Touched: installer/make_shortcut.py (new) + addon_cuda.py (new) + boot.py + build-bundle.ps1 + first_run.py + _dl.py + addon_image.py + downloads.json, selmo_image.py, QUICKSTART.md, README.md (status -> v0.928 beta).
- **v0.927** - docs only (chat.html badge stays v0.926): **MODELS.md moved and refreshed.** The "adding a model" guide moved from the untracked-feeling `installer\` into `docs\MODELS.md` as the tracked source of truth, and its recommended-models table was rebuilt against the current `selmo-models.ini` roster (Mistral-7B first-run default, EuroLLM-9B ethical default, Mistral Small 3.2 24B production default, Magistral 2509 reasoning+vision, Qwen3.5-2B, the LFM2 family, Gemma as benchmark-only) with role / license-origin / size / fit columns. Added a dedicated **Qwen3 30B/35B-A3B** section: the MoE `--n-cpu-moe` tuning (start ~26 and lower N while VRAM holds for more speed), >30 t/s on the 12 GB + 32 GB reference box, reasoning via the THINK toggle, and the caveat that llama.cpp vision for Qwen3 is still fresh. Fixed stale license notes (Gemma = Gemma Terms, not Apache; Qwen = Apache 2.0 but data-undisclosed and non-EU). Packaging: `installer\build-bundle.ps1` now ships MODELS.md from `docs\` to the **bundle root** (next to QUICKSTART.md) instead of `installer\`, and `QUICKSTART.md` step 2 points at `MODELS.md` in the Selmo folder. No app-code change. Touched: docs/MODELS.md (git-moved from installer/MODELS.md), QUICKSTART.md, installer/build-bundle.ps1.
- **v0.926** - Custom-profile instrument overhaul + presets carry their parameters. (1) **Gauge palette is now per-profile.** The dial face, ring and needle were hardcoded navy and ignored the Mizan/Custom palettes; they now read from CSS vars (`--g-face0/1/2`, `--g-ring`, `--g-nd0/1`) defaulting to Selmo navy, overridden to dark-red + amber needle for Mizan and a **light cream face for Custom**. The numbers, scale labels and tick marks moved to `--g-ink` / `--g-lab` / `--g-tickmaj` / `--g-tickmin` (defaults = the old grey/yellow), set to **black on Custom** so they read on the light face; SVG stops/ticks are driven via inline `style="...:var(--...)"` (Firefox resolves CSS vars on `stop-color`/`stroke`) - the main gauge in chat.html plus both mini gauges in selmo-core.js/selmo-boot.js. (2) **Dynamic status colours re-toned globally** so they show on navy, red AND cream: green `#55FF55` -> `#2f9e44` (darker), yellow `#FFFF00` -> `#c9a227` (ochre); red unchanged. (3) **Odometer lightened on Custom** - the black LCD was an eyesore on the light theme: light panel + cells, near-black lit digits/separator/unit, faint unlit; Selmo/Mizan keep the black display. (4) **Removed the redundant "Wh session" text row** from the dashboard (the odometer already shows session Wh) so the aside no longer overflows - the poll write to `#wh-s` is dropped, `resetWhSession` now zeroes the odometer via `setOdo(0)`, and the chat export header reads the `wh` global instead of the deleted element. (5) **Presets now carry their sampling parameters.** `savePreset` writes a YAML front-matter header (`temp`/`top_p`/`top_k`) above the system prompt; `onPresetFile` parses it back into the param fields (BOM/CRLF-tolerant) and loads the body, a header-less .md still loads as a plain prompt. chat.html + selmo-core.js + selmo-boot.js + selmo-chat.js + selmo-model.js (badge -> v0.926, `?v` for core/boot/chat/model -> 0.926); client-only, Ctrl+F5, no tray/bridge restart. (Build note: BUG-META-02 again - the bash mount served truncated views of selmo-core.js/chat.js, so `node --check` falsely failed on the last line; every edit verified with the Read tool, boot.js passed `node --check` intact.)
- **v0.925** - capability gating + optional add-on installers + phone-access gate + language-aware TTS. (1) Bottom-bar buttons grey out (aria-disabled, so their tooltip shows; clicks swallowed by a capture-phase guard) when their service/capability is missing - web/mic/vad/tts/image/vision; THINK now stays visible-but-disabled on non-reasoning models instead of vanishing; a `vision` flag added to selmo-config.json (server+tray). (2) Three independent add-ons built by build-bundle.ps1: Install-Voice / Install-Image (GPU-gated, sd.cpp isolated in bin\sd\ to avoid the ggml DLL clash) / Install-Hardware-Monitor, each with an Uninstall twin, sharing installer\_dl.py. (3) Phone-access toggle in the popup + LAN gate in selmo_https_proxy.py: non-loopback blocked when off, loopback-only /phone control API, default OFF on a public network (checked at startup). (4) TTS follows the detected text language (Kokoro via the bridge's langdetect; Web Speech via a client heuristic), respects the user's chosen --voice for its own language and preserves gender across languages, +10% Italian speed. (5) Fixes: phone icon + port 8443 dead on a clean bundle (added `cryptography` so the proxy can mint the cert); image-install PermissionError; English read with an it-IT voice. Touched: selmo-bridges/boot/media/model/sessions.js, selmo_tray/server/https_proxy/tts/image.py, installer\ (build-bundle.ps1 + _dl/addon_voice/addon_image/addon_uninstall.py), setup-lhm.ps1, QUICKSTART/landing.
- **v0.924** - portable bundle + first-run model download + CPU auto-tuning (backend/installer/docs only; zero client change, so the chat.html badge stays v0.922). (1) `installer\build-bundle.ps1` fixed: adds the previously-missing `selmo-i18n.js` and `fonts\` (without them the current chat.html breaks) and REMOVES every secret / per-machine runtime file from the copy list (`selmo.key`/`selmo.crt`/`selmo-cert-ip.txt`/`selmo-config.json`/`selmo-state.json`/`selmo-image-config.json`) - the old `dist\` shipped the live private key; closing hint moved from Inno Setup to `Compress-Archive` (Inno retired for a portable ZIP). (2) First-run OFFERS a default model: `installer\first_run.py` asks `Download the default model now? [Y/n]` after the engine step (skips if a .gguf already exists, declines on EOF so nothing huge is pulled non-interactively), reading a new `model` entry in `installer\downloads.json`. Default settled on **Mistral-7B-Instruct v0.3 Q3_K_M** (Apache 2.0, European, non-reasoning, ~3.5 GB - the lighter quant, for entry-level 8GB PCs) after rejecting LFM2.5-8B (reasoning locked ON) and Qwen3.5-2B (multimodal: hallucinated about images when loaded text-only, and too small to integrate web results). (3) CPU auto-tuning in the tray: `_build_cmd` (selmo_tray.py) now runs the srv through new `_adapt_srv_for_cpu()` - when `nvidia-smi` finds no GPU it rewrites `-ngl N` -> `-ngl 0` and drops `--no-mmap` / `--n-cpu-moe`, so a GPU-tuned ini section still loads well on a CPU-only 8 GB PC; unchanged on a GPU box. (4) llama.cpp CLI fix: the updated engine made `--flash-attn` require a value (`on|off|auto`); both Mistral ini sections passed it bare and llama-server refused to start (`expected value for argument`) - now `--flash-attn auto`. (5) Docs realigned (QUICKSTART.md, landing\quickstart.html, installer\MODELS.md): no bundled model, first-run offer, and the tray icon corrected to "the engine in the background" (the model is picked in the browser, not the tray). Verified via the Read tool throughout - the bash mount again served truncated views (BUG-META-02), so py_compile / json.load falsely failed on intact files. `dist\` still to be rebuilt on Windows with the fixed script. Touched: installer\build-bundle.ps1 + first_run.py + downloads.json + MODELS.md (installer\ still untracked in git), selmo-models.ini, selmo_tray.py, QUICKSTART.md, landing\quickstart.html.
- **v0.923** - docs only (chat.html badge stays v0.922): English book title and public domain finalized. English edition retitled from the literal *Dialogues with the Washing Machine* to **The Washing Machine Dialogues** - a noun-adjunct construction (not a genitive, which would be *The Washing Machine's Dialogues*, and not an apposition), in the "The ... Dialogues" title pattern (cf. *The Pentagon Papers* / *The Vagina Monologues*): shorter, more titular, reads as a named body of work; the dropped "with" is already carried by "dialogues" and can live in a subtitle if the ambiguity ("dialogues about vs with a washing machine") ever needs resolving. Public discovery domain moved in lockstep to `thewashingmachinedialogues.eu` (26 letters; the s31 literal `dialogueswiththewashingmachine.eu` is dropped). Trademark clearance recorded: the only exact "ANSELMO" on TMview is a Chinese CNIPA mark in Nice class 9 - territorial, no EU bearing - alongside ~200 "anselmo" variants (common Italian surname), so freedom-to-operate holds in the EU for a free, non-commercial, source-available release; no IP clause added to TERMS.md (section 11 already permits per-version updates if the project ever changes scale). Distribution plan for the landing: a static `index.html` on GitHub Pages, apex A-records to GitHub Pages IPs + `www` CNAME, custom domain with enforced HTTPS. Touched: README.md, TERMS.md, selmo-dev.md (this entry + checklist point 4), selmo-positioning.md (s31 addendum). No app-code change. **Domain availability still to be confirmed at the registrar** before purchase (`.eu` needs EU residency; Cloudflare Registrar is unreliable for `.eu`, use Namecheap/one.com).
- **v0.923** - pre-launch pass: brand, terms, educators note, local font. (1) **Public brand renamed SelmoAI -> A.N.S.E.L.M.O** (the exact name at the start of the book; official EN backronym from the translation in progress: "Algorithm for Neural Synthesis with Emotional-Linguistic Memory Optimization. Selmo, to friends."). Touched: chat.html title / header brand / loading-overlay title / profile-modal first badge / badge tooltip (+ i18n EN-IT now "Switch mode (A.N.S.E.L.M.O / Mizan / Custom)"), selmo_tray.py user-visible strings (tray tooltip, notifications, menu, console banner), LICENSE (Software: A.N.S.E.L.M.O ("Selmo"), Licensor: Fabio Garzetti), NOTICE (brand + backronym + attribution; also fixed the weights contradiction: the portable bundle MAY include third-party weights under their own licenses), README.md, QUICKSTART.md. Internal file names deliberately unchanged (zero legal benefit, high breakage risk); "Selmo" stays wherever it names the character/profile (Exit Selmo, the Selmo profile) - in the novel Selmo is A.N.S.E.L.M.O's nickname, so nothing is inconsistent. Rename motivated by a web clearance: "Selmo"/"SelmoAI" collide head-on with selmo.ai (active AI-search-visibility service), a Selmo call-center SaaS on G2/Capterra, Selmo Technology GmbH (industrial PLC software, AT) and selmo.io (social commerce, PL); "A.N.S.E.L.M.O" has only distant neighbours (anselmo.ai, a SF field-service consultancy; an aeronautical digital-twin platform) and public discovery will run through the book-title domain (dialogueswiththewashingmachine.eu - to be registered, mind the spelling). (2) **Central badge on two lines**: mode name big (A.N.S.E.L.M.O / Mizan / Custom) + small cyan MODE underneath (new `.wm-mode` CSS); `setProfile` (selmo-model.js) now writes the same two-line markup on every switch, which was also where the old 'SelmoAI' kept resurrecting after the static-markup rename. (3) **Welcome bubble trimmed** to "New conversation." - the "Only explicit web searches..." line removed; markup re-keyed to i18n `welcome.line1`, orphan `welcome.full` dropped from the dict. (4) **Google Fonts removed**: Share Tech Mono is now self-hosted (`fonts\share-tech-mono-latin-400-normal.woff2` + `fonts\LICENSE-OFL.txt`, from the @fontsource npm package) via an inline @font-face replacing the fonts.googleapis.com link - no user IP to Google (a German-caselaw GDPR sore point), CSS fallback chain unchanged. (5) **TERMS.md added** (Mozilla-style Terms of Use, EN: no-telemetry / no-backdoor section with the exhaustive verifiable list of what leaves the machine, AS-IS warranty disclaimer, liability cap with the art. 1229 c.c. carve-out, Italian law + Court of Milan venue with the mandatory consumer-forum carve-out) and **educators note** in QUICKSTART.md (any t2i model is easy to push to inappropriate output and a local model has no cloud filter: with minors keep `image\` empty and supervise), mirrored as a "Minors" bullet in TERMS section 6; README/QUICKSTART privacy claims realigned to the truthful list (first-run engine download, CDN libs, explicit web search - "nothing leaves the machine" as previously worded was false on first load). (Build notes: BUG-META-02 struck twice - stale bash views made `node --check` falsely fail on selmo-model/i18n.js and served pre-edit byte counts for the .md NUL checks; every edit verified with the Read tool instead. **dist\ NOT rebuilt**: it still carries the old brand, the Google Fonts link, selmo-state/config, selmo-cert-ip.txt and - critical - the live `selmo.key`; see the Pre-launch checklist.) chat.html + selmo-model.js + selmo-i18n.js + selmo_tray.py + LICENSE + NOTICE + README.md + QUICKSTART.md + TERMS.md (new) + fonts\ (new) + selmo-positioning.md (addendum); client Ctrl+F5, tray restart for the renamed strings.
- **v0.923** - i18n phase 1: the localization engine, dormant by default. New `selmo-i18n.js` (loaded FIRST, before every other module) defines `t(key,vars)`, `setLang(code)`, `applyI18n()` and the `I18N` dictionary (semantic dotted keys -> {en, it}) plus an `I18N_BIND` table mapping header/input/profile chrome elements to keys by CSS selector. **Safety contract:** the active language defaults to 'en' and `applyI18n()` is NOT called on load unless a non-'en' language is persisted, so in the shipped state the DOM is never touched and the page renders exactly as before; the file is also purely additive (no existing .js is wrapped in `t()` yet), so even a load failure cannot affect the running app. Localization is proven from the console: `setLang('it')` flips the chrome to Italian, `setLang('en')` restores it. Coverage: the header + input chrome (explicit selector bindings) plus a generic `[data-i18n]` / `[data-i18n-title]` / `[data-i18n-ph]` sweep driven by attributes added directly in the markup - now reaching the left sidebar label, the whole power/stats dashboard (system power draw, session watt-hours, tok/sec, Wh session/total, session/total cost, euro/kWh, tokens generated), and the welcome bubble (both the static markup and the JS-regenerated `New conversation.` in selmo-model.js, wrapped in a `typeof t` guard so it falls back to the literal if i18n is absent). The model's reply language stays governed by the system prompts, a separate axis. A globe language picker sits in the header toolbar (`_i18nPicker`, added to `.h-left`): the globe button opens a CENTERED popup (`_openLangPopup`, same technique as the phone-access popup - robust on mobile and at any window size, replacing a first anchored-dropdown attempt that positioned badly) listing SELMO_LANGS with a check on the active language, wired to `setLang()` so the choice persists across sessions via localStorage and applies immediately; it is the one intentional always-visible addition to the default UI. Deliberately deferred to phase 2: the remaining dynamic .js message sites (status chips idle/web off/..., toasts, errors), img-menu / profile badges / param tooltips, externalizing the dict to `selmo-i18n.json` for the tray to share, and fixing the residual EN/IT drift. The globe button (and the v0.920 phone button) are styled inline in JS to match the other header buttons (`#web-btn` look: subtle bg, steel border, dim text, cyan hover) - only NEW CHAT stays inverted; because the palettes (Selmo/Mizan/Custom) remap the `--steel`/`--dim`/`--cyan` variables with no per-id button overrides, the icons adapt to all three automatically. The two popups (language + phone) had their card text switched from a hardcoded light `--fg` fallback to `var(--white)` (which the palettes invert), so they stay readable on the light Custom theme instead of rendering light-on-light; done inline rather than via a `#lang-btn,#phone-btn` CSS rule because the sandbox mount was serving a stale/truncated view of chat.html and a Python-in-bash write would have chopped it (BUG-META-02), so all header-icon work stayed in the .js via the Edit tool. Minor debt: an inert dead-code block (old anchored-dropdown `menu`/`rebuild`, never attached, never called) remains inside `_i18nPicker` - the Edit tool could not match it under the stale mount; to be removed on a fresh mount. chat.html (new <script> tag first in the block + 13 `data-i18n` attributes, badge -> v0.921) + selmo-i18n.js + selmo-model.js + selmo-sessions.js (`?v` -> 0.921 for model + sessions); client-only, Ctrl+F5, no tray/bridge restart. Final maquillage: the footer meta line (`Only web searches transmitted / <model> / localhost:8080`) was removed from chat.html entirely; its `#foot-model` updater in selmo-boot.js is already `if(fm)`-guarded, so nothing breaks. Plan and rationale in selmo-i18n-plan.md.
- **v0.920** - phone-access button in the header. A 📱 button (appended to `.h-left` by `_phoneField` in selmo-sessions.js) appears only on the desktop/localhost view; clicking it opens a popup (`_openPhonePopup`) with the LAN address to type in the phone's browser - `https://<ip>:8443/chat.html` - plus a copy button and a one-line note (same Wi-Fi, accept the cert warning once for the mic). The IP is read from the front door's `selmo-cert-ip.txt`; if there is no valid IP the button is skipped. Built entirely in JS (no chat.html surgery): `_phoneField()` is called from selmo-boot.js right after `_micHttpsBanner()`. Replaces a first attempt that inserted a full-width top bar, which got squeezed into the sidebar and broke the layout. selmo-sessions.js + selmo-boot.js + chat.html (badge -> v0.920, `?v` for sessions/boot -> 0.920); client-only, Ctrl+F5, no tray/bridge restart. **Security hardening committed together (not yet field-tested):** every backend bridge now binds 127.0.0.1 instead of 0.0.0.0 - only the front door stays on the LAN, and the client already talks solely through it via /proxy/808x, so zero functional change; the front door now rejects any dot-segment request path, so the whole `.git/` directory (its full history) is no longer downloadable over the LAN, and internal `.md` docs are blocked; `selmo_web.py` `/fetch` refuses non-http(s) schemes (it was an SSRF that read local files via file://). One item left open for Fabio's call: the front door itself is unauthenticated on the LAN (SEC-3). Files: selmo_https_proxy.py, selmo_web.py, selmo_whisper.py, selmo_tts.py, selmo_image.py, selmo_gpu_monitor.py, selmo_tray.py. Full write-up in SECURITY-REVIEW.md. **Docs added:** QUICKSTART.md (portable unzip-and-run install), SECURITY-REVIEW.md, selmo-i18n-plan.md (multilanguage strategy), selmo-positioning.md, selmo-feature-matrix.md.
- **v0.919** - open-models-folder button in the Models & runtime panel (desktop host only). A **📁 Models** button sits in the header row above each model list and opens that folder on the host in Windows Explorer - `models\` for the LLM list, `image\` for the image list. New tray control-API route `POST /reveal` (`_reveal` in `selmo_tray.py`): it takes a fixed `{kind}` enum (`llm`/`image`), `mkdir(exist_ok=True)` the target folder, and `os.startfile(folder)` it. Three deliberate choices: (1) it opens the **folder itself, not a selected file**, so it works on a fresh install with **no models yet** (the first redesign revealed a specific model file via `explorer /select,` and so was useless precisely when you most need it - an empty install); (2) `os.startfile` uses the shell "open" verb to raise an Explorer window; Windows blocks a background process (the tray) from stealing the foreground, so it may only **flash in the taskbar** rather than pop up. We deliberately do NOT fight this (a foreground-lock override mutates a system-wide setting for little gain) - instead the client shows an **in-app confirmation popup** (`#reveal-note`, "Model/Image folder shown, check your taskbar." + an OK button, `showRevealNote`/`closeRevealNote` in `selmo-settings.js`) so the user knows it worked and doesn't keep clicking; (3) `kind` is a fixed enum and the path is built server-side, so nothing arbitrary reaches the unauthenticated 8087 port. Layout: the LLM and image sections are now **side by side** (`.set-cols` two-column flex, settings card widened to 680px) instead of stacked; the active-item selection ring was being clipped left/right (the list's `overflow-y:auto` forces `overflow-x` to clip, so the 2px cyan ring showed top/bottom in the gaps but was cut on the sides, reading as a taller-than-wide box) - fixed with horizontal padding on `.set-list` so the ring has room on all sides. The list scrollbar was also jammed against the edge and hard to see; `scrollbar-gutter:stable` + a thin cyan `scrollbar-color` (Firefox) give it a reserved, visible gutter; on tablet+phone the columns restack and the `desk-only` folder buttons hide (the existing device media query) - opening a host folder is meaningless from a remote device. Client `revealModel(kind)` in `selmo-settings.js` POSTs `{kind}`. selmo_tray.py + selmo-settings.js + chat.html (badge -> v0.919, settings `?v` -> 0.919); restart the tray for the new route (the 8087 server runs in the tray process), then Ctrl+F5 the client. **Also landed in this version:** (1) Enter now follows whichever mode the header shows instead of always sending chat - in `image mode` it calls `toggleImgMenu()` (same as clicking the palette: generates another image, or opens the img2img picker if an image is loaded), otherwise it sends chat as before (`handleKey`, selmo-chat.js). (2) Dropped the "Model unloaded for an image - click to reload" revive banner in image mode (`setModelState('imaging')`, selmo-boot.js): it invited a premature one-click reload back to the chat LLM while the user was still meaning to generate images, undoing the point of staying in image mode - Enter and the palette button keep working without it, so the banner did more harm than good; the header label + yellow connection dot still flag image mode on their own. (3) `selmo-models.ini` tooltips (`tip=`) rewritten from the bird-metaphor flavor text to plain, literal descriptions of what each model does. (4) `selmo_server.py`: llama-server binary directory is now overridable via `SELMO_BIN_DIR` (default `bin`, unchanged), letting a build pick a `bin-vulkan` backend at runtime without touching the production launcher.
- **v0.918** - interruptible TTS. You can now cut Selmo off mid-speech: the STOP button (which used to vanish the moment generation ended) reappears while TTS is playing and stops it, freeing the UI for the next turn. `speakText` (selmo-media.js) now stores the active Kokoro `AudioContext`/`BufferSource` and a fetch `AbortController` in new globals (`_ttsCtx/_ttsSrc/_ttsAbort/_ttsFire`, selmo-boot.js); new `stopTts()` aborts the pending fetch, stops the source, closes the context, cancels the Web Speech fallback, and runs the same cleanup as a natural end (resets `_ttsPending`, restores the send button via `_ttsShowStop`, calls `vadAfterSpeak`). `stopMsg()` (selmo-chat.js) and the start of `sendMsg()` (selmo-send.js) both call `stopTts()`, so the single STOP button covers both streaming text and speech, and sending a new message cuts off any speech still playing. **Deliberately NOT a conversation-mode (VAD) interruption** - the mic/VAD path is untouched, so background noise can never cut the voice; stopping is a manual action only (the failure mode that wrecks most always-listening assistants). selmo-media.js + selmo-boot.js + selmo-chat.js + selmo-send.js + chat.html (badge -> v0.918, `?v` for boot/media/chat/send -> 0.918); client-only, no tray/bridge restart. (Build note: BUG-META-02 struck again - bash served pre-edit truncated views of all four .js after the file-tool edits, so `node --check` on the mount falsely reported "Unexpected end of input"; the real files were intact per the Read tool, and the new TTS block passed `node --check` in /tmp.)
- **v0.917** - docs only (chat.html badge stays v0.916). Added a **Linux / macOS port** entry under Nice-to-have / future work, marked community-supported and post-distribution: records what ports cleanly (client + Flask bridges + native llama.cpp builds) vs what needs rework (shell launcher, headless tray rewrite, non-NVIDIA power source, per-platform image binaries), and documents the s29 decision to **reject a Raspberry Pi 8 GB target** (no inference GPU, 8 GB cap = 1.5-4B models only, image gen impractical) with Apple Silicon flagged as the stronger first port. No app-code change.
- **v0.916** - image mode is an ACTIVE state, not a dead one, plus polling/reload robustness. (1) `setModelState('imaging')` no longer adds `body.unloaded`, so image mode keeps the normal palette (NO grey); the grayscale stays reserved for `unloaded` / "no model". Image mode is flagged by the `image mode` label + yellow dot + the revive banner. (2) `checkServer` now keeps polling after `ready` (re-poll every 60s) and reconciles the header against the real backend (`/v1/models` name vs tray `/status`), so it never falls behind a swap/switch/unload triggered by another sequence or device; it skips reconciling while an image is generating (`gen` guard) and `genImage` flips the header to image mode instantly. (3) New one-click **revive banner** in the grey states (`_showReviveBanner` / `reviveModel`): reloads the LLM directly so you no longer have to type a throwaway message to wake it. (4) A `_reloading` flag makes the polling failure path show the loading thumbs (not grey "no model") during a deliberate LLM reload, in both `reviveModel` and `ensureLLM`. selmo-boot.js + selmo-media.js + selmo-chat.js + chat.html (badge -> v0.916, boot/media/chat `?v` -> 0.916); client-only, no tray restart. Also: parallel-chunking design (compare two versions of the same text) parked under Nice-to-have / future work. (Recovery note: a Python-in-bash write hit BUG-META-02 mid-session and truncated selmo-boot.js; restored from `git show HEAD:` + re-applied, `node --check` run in /tmp to dodge the stale mount.)
- **v0.915** - fix: reloading the page during image generation no longer shows the twiddling-thumbs "loading" overlay. The VRAM swap unloads the LLM during image gen (`swapped_for_image`), so a reload found `/v1/models` down and treated it as a model load. Now `checkServer`'s failure path queries the tray `GET /status`: if `swapped_for_image` is true it enters a new **`imaging`** state (grey palette, header `image mode`, yellow dot, NO overlay) instead of the loading thumbs or the "no model" state; it keeps polling and recovers to `ready` once the LLM reloads (next chat via `ensureLLM`). selmo-boot.js only (badge -> v0.915, boot `?v` -> 0.915); client-only, no tray restart.
- **v0.914** - tooltips. (1) Fixed the image-generation button tooltip: it hardcoded "Z-Image-Turbo" (wrong now that the image model is selectable) -> "Generate an image from your prompt - click to choose the mode (text-to-image / img2img)". (2) Recovered the per-model hover tooltip in the browser panel (the tray used to show the ini `tip`/`note` on hover): `GET /models` and `GET /image/models` now include each model's `note` + `tip` from the ini match, and `selmo-settings.js` sets `it.title = tip || note` on every list item, so hovering a model in the panel shows its description like the old tray Listbox did. selmo_tray.py (control API) + selmo-settings.js + chat.html (tooltip text, badge -> v0.914, settings `?v` -> 0.914).
- **v0.913** - unload now shows an inactive (grey) palette, not the loading overlay. A `setModelState('loading'|'unloaded'|'ready')` state machine in `selmo-boot.js` replaces the v0.912 "actionable card": **loading** = the twiddling-thumbs overlay (only when a NEW model is coming up), **unloaded** = `body.unloaded` greys the UI (`filter:grayscale` on main/nav/aside/topbar/`.h-left`/`.h-center`) while the **model button stays lit** (cyan glow) as the call-to-action, header reads `no model`, no overlay; **ready** = normal. `checkServer` drives it (success->ready, <7 fails->loading, >=7 fails->unloaded). `selmo-settings.js`: `unloadLLM()` -> `setModelState('unloaded')`, `reloadLLM()`/`confirmSwitch()` -> `setModelState('loading')`. So the thumbs appear only during an actual load; a deliberate unload greys out and invites loading a model. chat.html (`body.unloaded` CSS, badge -> v0.913, boot/settings `?v` -> 0.913) + selmo-boot.js + selmo-settings.js.
- **v0.912** - fix: the model-loading overlay could trap the user. When the model was **unloaded on purpose** (panel Unload) or the page was opened while no model was loaded, `checkServer` kept failing and re-showing the full-screen "twiddling thumbs" overlay (z-index 400) every 2 s, covering the header so settings couldn't be reached - and the header never showed an unloaded state. Fix (`selmo-boot.js`): the overlay is now non-trapping - after ~14 s (`_checkTries>=7`) with no model it turns into an actionable card ("No model loaded -> Open settings to load one", thumbs hidden) and the header shows `no model`; a `_overlayDismissed` flag means a tap-to-dismiss (backdrop) or the Open-settings button permanently stops the overlay from re-appearing, while polling keeps running and auto-recovers if a model comes up. `selmo-settings.js`: `unloadLLM()` now sets the header to `no model` + red dot for immediate feedback; `reloadLLM()` does a `location.reload()` once the model is back so the header refreshes. chat.html (overlay markup + `.ml-btn` CSS, badge -> v0.912, boot `?v` -> 0.912) + selmo-boot.js + selmo-settings.js.
- **v0.911** - mobile bottom toolbar justified: on ≤640 the action buttons (`#upload-btn` `#upload-img-btn` `#send` `#img-wrap` `#mic-btn` `#vad-btn` `#tts-btn` `#stop`) get `flex:1 1 auto` so they stretch to fill each wrapped row instead of leaving empty space on the right; `#gen-img-btn` fills its `#img-wrap`. chat.html only, badge -> v0.911. (Possible future polish, noted but not done: group the toolbar logically into input / output / modes.)
- **v0.910** - mobile header on two rows, brand + model together (final form; supersedes the v0.904 overflow and the v0.907-09 iterations). On any device view (`≤1024px` / coarse pointer) the header `flex-wrap`s via `order`/`flex`: **row 1** the brand on the left (`.h-center{order:-2;flex:1 1 auto;justify-content:flex-start}`) and the active model on the right (`.h-right{order:-1;flex:0 0 auto;justify-content:flex-end;max-width:58vw}`) — identity + what's running, on one line; **row 2** the action buttons justified edge-to-edge (`.h-left{order:0;flex:0 0 100%;justify-content:space-between}`). `--header-h` -> 86px on device. Kept the robustness CSS: `header{overflow:hidden}`, `.h-left{overflow-x:auto;scrollbar-width:none}`, `.h-right` clipping. Usage topbar thinned (`--topbar-h` 66->48, `.tb-lbl` 10->9px); ≤400 NEW CHAT -> "+". Desktop unchanged (single 3-zone row). chat.html only, badge -> v0.910.
- **v0.906** - model-loading overlay + auto-recovery (fixes a long-standing regression). On a cold start the front door serves `chat.html` before llama-server is up, so the page used to settle on the `local` fallback and needed a manual refresh once the model finished loading. `checkServer` (`selmo-boot.js`) now (a) treats a `/v1/models` 200 with no model id as "still loading" and keeps polling (every 2 s) instead of latching onto `locale`/`local`, and (b) auto-recovers the header model name + `loadProps()` the moment the model answers - no refresh. While loading, a full-screen overlay (`#model-loading`) shows an animated SelmoAI "twiddling thumbs" (inline SVG + CSS, no asset, offline-friendly); it appears only if the model isn't up within 500 ms (no flash on a normal refresh) and can be clicked to dismiss if a load ever hangs. chat.html (overlay markup + CSS, badge -> v0.906) + selmo-boot.js (`?v` -> 0.906); client-only, no tray restart needed.
- **v0.905** - tray-in-browser, step 2 + tray slimmed down. (1) **Image model** switch from the panel, mirroring the LLM one: `GET /image/models` + `POST /image/select` (writes `selmo-image-config.json`); `selmo_image.py` now re-reads its config on every `/generate`, so the switch applies without a bridge restart. (2) **Editable flags at switch**: `GET /models` returns each model's `srv` + `chunking_size`; `POST /llm/switch` accepts `{srv?, chunking_size?}` overrides; `_action_switch` got the override params. The panel shows a textarea with the srv string + a chunking field before you Load (the old tray "show + retype", now in the browser). (3) **Tray no longer auto-picks via a dialog**: `main()` drops `_gui_picker` and auto-loads the **last-used model** from `selmo-state.json` (`_load_last_model`/`_save_last_model`, written on every switch); services start regardless, model loads if found. The tray menu lost the "Switch model" submenu (browser-only now). chat.html panel got the LLM/image editors + CSS; `selmo-settings.js` rewritten; `?v` for it -> 0.905, badge -> v0.905. selmo_tray.py + selmo_image.py + chat.html + selmo-settings.js + docs. (Note: BUG-META-02 made bash serve a stale committed view of `selmo_tray.py` the whole session, so `py_compile` couldn't see the edits; verified each edited region via the Read tool instead.)
- **v0.904** - header redesign + mobile model access. The header is now three zones: **left** = menus/conversations (history toggle, NEW CHAT, EXPORT, WEB, THINK, dashboard toggle), **center** = the SelmoAI brand, **right** = the model + session/conn. The model is now a real `#model-btn` (not a `.hstat`), so it stays **visible on mobile** - previously the tablet/phone breakpoint hid `.hstat` and the model button vanished, which was the most-used platform. The button truncates with an ellipsis and drops its "model:" prefix on phones. Removed the **GDPR badge** (it read as an implicit guarantee). chat.html only (markup + CSS + badge -> v0.904); no JS change, so the `.js` `?v` stays 0.903.
- **v0.903** - tray-in-browser, step 1: change the LLM model from any browser. `selmo_tray.py` control API (8087) gained `GET /models` (lists `models\` + current/loaded), `POST /llm/switch` (`{name}` -> reuses the existing `_action_switch`), and `POST /control/exit`; `/llm/unload` `/llm/reload` `/status` already existed. New client module `selmo-settings.js` (pure functions): a "Models & runtime" panel opened from the header `model:` label, listing the models with the current one highlighted, plus Unload / Reload / Exit buttons; after a switch it polls `/props` until the new model is up and then reloads the page so ctx/think/config refresh. chat.html gained the panel markup + CSS (reusing the profile-modal classes) and the `selmo-settings.js` tag (before `selmo-boot.js`); all script tags bumped to `?v=0.903`. Still TODO: image-model pick + srv/chunking editing. NB: 8087 is unauthenticated on the LAN. (Build note: hit BUG-META-02 again — after editing `selmo_tray.py` with the Edit tool, bash served a stale truncated view and `py_compile` falsely failed at a line in `_build_menu`; the Read tool confirmed the real file was intact and balanced.) selmo_tray.py + chat.html + new selmo-settings.js + docs.
- **v0.902** - `selmo-app.js` subdivided into per-subsystem modules (completes the v0.900 split plan). The single ~116 KB script became 8 function-only modules (`selmo-core/bridges/model/docs/chat/send/media/sessions.js`) plus `selmo-boot.js` (all globals + top-level init, loaded LAST). chat.html loads all 9 in order with a shared `?v=0.902`; `selmo-app.js` removed. Key lesson: a first attempt at a contiguous byte-identical split **broke** because classic scripts don't share function hoisting across files - top-level init in an early file called functions defined in a later file (`renderSessionList`, `syncThinkPrompt`) -> ReferenceError, and reordering can't fix the cycle (early init needs late functions; late init needs early globals). Fix = separate ALL definitions (modules, load order irrelevant) from ALL execution (boot, last). Built with a walker that routes each top-level function to its module and everything else to boot; verified by node --check on all 9, a no-lines-lost reconstruction (2340/2340) against the original, and a clean Firefox console (only the unrelated favicon.ico 404). chat.html (9 tags + badge) + 9 new .js + docs; selmo-app.js deleted.
- **v0.901** - Qwen no-answer fixed (BUG-NOANS-01 residual, on top of the v0.836 history strip). Two changes in `selmo-app.js`, both in the chat send path. (1) `streamTokens`: the `reasoning_content` and `content` SSE branches were an `if/else if`, so a single delta carrying BOTH (Qwen often bundles the tail of the think block with the first answer tokens, and a short answer can ride entirely on combined deltas) dropped the answer -> 0 tok. Made the two branches independent `if`s, so no field is ever dropped. (2) No-think routing: when the THINK toggle is OFF (`!IS_THINK_ON`), any model output that still arrives as `reasoning_content` is now forced into the answer bubble instead of the reasoning panel; the panel is created lazily and `seal()` removes it when empty, so no stray empty panel shows. Net: with Qwen, THINK-off answers land in the bubble, and THINK-on multi-turn conversations answer on every turn. The chunk pipeline's own `streamTokens` call (reasoning intentionally kept out of the document stitch) was left untouched. Cache-bust `?v=0.901` + badge. selmo-app.js + chat.html (badge/?v) + docs.
- **v0.900** - client split groundwork: the entire inline `<script>` was extracted from `chat.html` into a new `selmo-app.js`, loaded with a classic `<script src="selmo-app.js?v=0.900">` in the exact same document position. Byte-identical, behaviour-identical refactor (proved by reconstructing the original bytes from the two files; `node --check` clean; 0 NUL; confirmed answering on Mistral). chat.html drops 158 KB -> 42 KB; the front door already serves `.js`, cache-busted via `?v=`. No logic changed - the per-subsystem subdivision of `selmo-app.js` (selmo-core/chat/profiles/docs/web/voice/vision/imagegen) is deferred to follow-up commits on the pure `.js`. New "Client file map" section near the top documents the module layout and is the guide for future development. (Reasoning-model no-answer seen on Qwen during testing is pre-existing BUG-NOANS-01, unrelated to the split; queued as v0.901.)
- **v0.839** - chunk-pipeline context guard (BUG-CHUNK-01, under observation). Chunked tasks were silently capping every chunk at ~512 output tokens and losing most of the text, because `N_CTX` could stay at the fallback 4096: the real ctx is read from `/props` once at page load and that fetch had no retry, so a late/failed `/props` (e.g. the LLM swapped out for image generation, `ensureLLM` reloads it but never re-reads `/props`) left `maxTok = max(512, (4096-overhead)-chunking_size)` floored to 512 while the input chunk still filled the window. New `MODEL_READY` flag (true only when `loadProps()` reads `n_ctx >= 1024`); `loadProps()` retries `/props` up to 10x and is re-called from `ensureLLM()` after a post-image reload so `N_CTX` refreshes. Pre-flight guard in BOTH pipelines: `processDoc` aborts on `!MODEL_READY || maxTok<=512`, `processChunks` on `!MODEL_READY`, showing a neutral "model not ready, Ctrl+F5 and retry" message instead of burning energy on capped chunks. chat.html only; documented in selmo-bug-report.md, left under observation pending the runtime checks.
- **v0.838** - bird codenames + hover tooltips (all ini-driven) and a ctx/chunking pass. Every model now leads its `note=` with a bird name and compact tags that fit the launcher row: vision models are raptors (Golden Eagle/Qwen3, Hawk/Magistral, Owl/gemma, Merlin/LFM2-VL), text models named by trait (Swift/LFM2.5, Swallow/LFM2, Sparrow/Mistral-7B, Raven/Mistral-24B, Robin/Olmo, Crane/EuroLLM-22, Starling/EuroLLM-9B). A new SELMO-only `tip=` key carries the long description; the tray (`_parse_ini` + `_parse_image_ini`) reads it and both the LLM and image Listboxes gained a hover Toplevel tooltip that shows `tip` (falling back to `note`) for the row under the cursor - zero names or descriptions hardcoded in the launcher. Context/chunking tuning on `selmo-models.ini`: Swift ctx 8192->16384 and chunking 8192->5000 (chunking equalled the whole ctx, leaving no output room and overflowing on a native-reasoning model - the likely cause of its truncated translations), Swallow ctx 8192->16384 (4000-token translation chunks were truncating the output), Starling chunk 1000->1500, Crane 1200->3000, Raven 2000->3000 (all had idle output headroom; fewer chunks = fewer round-trips). Qwen3 left untouched (already optimized). ini + tray; chat.html badge only.
- **v0.837** - ini-driven, generalized THINK control. Reasoning on/off is no longer guessed from the chat template; each reasoning section in `selmo-models.ini` declares a new SELMO-only `think` key (`instr` Magistral / `kwarg` Qwen3 / `native` Olmo-Gemma; omitted = non-reasoning, blank = fall back to template auto-detect). The tray (`_parse_ini` + both launch sites) writes it into `selmo-config.json`; `selmo_server.py` gained a matching `--think` arg. `chat.html` reads `cfg.think` into `THINK_MODE` and a new `configureThink()` derives the control flags from it (template sniff kept only as the undeclared fallback). The THINK button is now the single source of truth: `instr` injects/omits the system instruction, `kwarg` sends `chat_template_kwargs.enable_thinking` (gated strictly to that mode), `native` locks the button ON (no real off), and the user's choice persists in `localStorage('sthink')`. Fixes the Magistral think-off regression: `markThinking()` no longer latches the toggle back on for declared/controllable models (the v0.806 "model is in charge" behaviour now only applies to an undeclared reasoner behind `[default]`), so a stray reasoning token can't break natural conversation. `REASON_FIRST` stays template-derived (it is a stream-parsing hint, not a control). chat.html + ini + tray + server; no llama-server flag added.
- **v0.836** - no-answer fix + device usage bars. (1) BUG-NOANS-01: assistant turns were sent back to the model with their `[THINK]reasoning[/THINK]` baked in, so from the 2nd turn a reasoning model saw a finished think block, opened a fresh one and stopped - zero answer tokens (any model, any ctx size). New `apiMessages()` strips `[THINK]...[/THINK]` from assistant turns at send time; `chatHistory` stays intact so the panel and saved sessions are unaffected; both streaming send-sites use it (chunk pipeline already had its own clean history). (2) The tablet+phone usage topbar (`#gpu-topbar`) was phone-only (<=640px) so on a landscape tablet - where the dashboard is an off-canvas drawer - no usage readout showed at all; it now appears across the whole tablet+phone breakpoint and the breakpoint itself was widened to cover touch tablets in landscape (`(max-width:1366px) and (pointer:coarse)`). (3) The topbar became three stacked rows of segmented usage bars: SYS watts (full width), then CPU% | GPU%, then RAM% | VRAM% (VRAM under GPU), each green/yellow/red by load, fed from the existing 8082 monitor. `--topbar-h` 16 -> 66px. chat.html only; no bridge change.
- **v0.835** - distribution readiness (repo/docs; no app-code change - the chat.html badge moves in lockstep only). Made the repo publishable: purged the committed private TLS key (`cert/selmo.key`) and 363 MB of model weights (`tts/`, `voices/`) plus old renamed/backup junk from ALL history with `git-filter-repo`, and scrubbed the hardcoded email from history; the pack shrank from ~330 MiB to ~0.5 MiB. Added `LICENSE` (Apache-2.0 + Commons Clause = source-available: use/modify/redistribute but no Sale), `NOTICE` (mandatory attribution to the Selmo project and the book *Dialoghi con la lavatrice*, propagated via Apache section 4(d)), and `README.md`; de-personalized `setup-git.ps1`; dropped the deprecated `backup.bat` from tracking. GitHub publication deferred to the v1.0 push.
- **v0.834** - clipboard + robust history. Paste an image into the input (`handlePaste`, sharing the new `addImageFiles` pipeline with the + IMG/OCR picker) attaches it for vision exactly like the button; non-image pastes still paste as text. Each reply gains a `copy` button next to the .md/.tsv downloads (`copyToClipboard`: async Clipboard API on a secure context, hidden-textarea `execCommand` fallback on plain HTTP so it works on the phone over 8080). History rationalized: stored turns are normalized to a string + an image COUNT, so a reloaded multimodal turn can never render `[object Object]` and its text is never lost; base64 image data is no longer written to `localStorage` (a placeholder marker stands in), which fixes the silent quota failure that made image sessions vanish on the phone, and `saveSession` now evicts the oldest OTHER session and retries on a quota error instead of dropping the current conversation silently. Reloaded assistant turns now render markdown and get the copy/download bar (previously deferred in v0.826).
- **v0.833** - Z-Image-Base tuning fix: the `[z-image]` section was undercooked (20 steps, no sampler/flow-shift), so Base looked worse than Turbo. Bumped to `--steps 30 --cfg-scale 4.0` and added `--sampling-method euler --flow-shift 3` (the proven flow setup from `[qwen-image]`); cfg 5.0 -> 4.0 to stay off the over-saturation edge. Note rewritten: Base is for finetuning/creative breadth, not a photoreal upgrade over Turbo (per Tongyi). ini-only change, no code.
- **v0.832** - more image models, all on the existing pipeline. New `[qwen-image]` section (Qwen-Image-2512, 20B, Apache 2.0: `--llm` Qwen2.5-VL-7B + `qwen_image_vae`, `--sampling-method euler --flow-shift 3`, cfg 2.5) and a `[z-image]` Base section (the non-distilled 6B sibling of Z-Image-Turbo, cfg 5.0, reusing Turbo's Qwen3-4B encoder + `ae.safetensors`, so only the diffusion file is an extra download). New `offload` ini key threaded ini -> tray -> bridge: `offload=always` forces `--offload-to-cpu` even on the freed GPU, for models too big to stay resident on 12 GB (Qwen-Image's 20B). The image scan now keys on `instruct` instead of bare `qwen`, so the Qwen-Image diffusion file is selectable while the Qwen2.5-VL / Qwen3 text encoders stay hidden.
- **v0.831** - single front door on port 8080 (`selmo_https_proxy.py`): it now serves `chat.html` and the static files itself and reverse-proxies every backend by port (`/proxy/8089` -> llama-server, `/proxy/808x` -> the bridges), so the UI always talks to one origin no matter which backend is loaded. Fixes the bug where loading the image model killed 8080: the v0.830 VRAM swap unloads llama-server (moved to private `127.0.0.1:8089`) without taking the page host down - while swapped, `/proxy/8089` returns 502 and `ensureLLM()` reloads on the next chat, but the page itself stays served. The front door listens on HTTP 8080 (desktop/localhost secure context) and HTTPS 8443 (phone), threaded, and auto-regenerates the TLS cert when the LAN IP changes (recorded in `selmo-cert-ip.txt`). `chat.html` backends moved to relative `/proxy/808x` paths (`API` = `/proxy/8089`); a mic-needs-HTTPS banner with a tap-link to the 8443 page appears when the phone loads over insecure 8080. The static server refuses to hand out `*.key`/`*.py`/`*.ini`/`*.bat`/`*.log`. Streaming uses HTTP/1.0 close-delimited framing so token streams terminate cleanly through the proxy.
- **v0.830** — VRAM swap for image generation: the bridge unloads the LLM before generating so the image model runs on the full GPU (no more `--offload-to-cpu` streaming by default), and the LLM reloads lazily on the next chat. New tray **control API on 8087** (`/status` `/llm/unload` `/llm/reload`, stdlib `http.server`, serialised on `_ctrl_lock`); image bridge POSTs `/llm/unload` + a 2 s CUDA settle, falls back to `--offload-to-cpu` if the API is unreachable (`--no-swap` forces the fallback, `--no-offload` forces GPU). `chat.html` gained `ensureLLM()` — reloads the LLM before a chat turn when `swapped_for_image` is set (tray-authoritative, so a phone-triggered swap heals on desktop), with an animated braille spinner so the reload never looks frozen. While in image mode the WEB/THINK buttons grey out (`setImageMode`, fired the instant generation starts), and the palette button goes straight to text-to-image when no image is loaded (one click less). `/proxy/8087` added to the HTTPS proxy.
- **v0.829** — image-gen mode picker embedded in the palette button: a popup menu (Text-to-image / Img2img Subtle·Medium·Strong) that opens upward and closes on outside-click, replacing the v0.828 `<select>` for a cleaner mobile row. (Recommitted onto v0.825 after a `.git` tree corruption — `fatal: empty filename in tree entry` — caused by running index-touching git from the sandbox; see Lessons. All writing git now stays on Windows.)
- **v0.828** — image-to-image: `/generate` accepts a base64 `init_image` + `strength`; bridge appends `--init-img <path> --strength <s>` under sd.cpp's default `img_gen` mode (the old `-M img2img` mode was removed); Subtle/Medium/Strong presets (0.4/0.55/0.7) reuse the loaded `+ IMG/OCR` image at its native aspect; temp init cleaned up.
- **v0.827** — local image generation: new `selmo_image.py` bridge (port 8086) wrapping stable-diffusion.cpp + Z-Image-Turbo (Apache 2.0), `--offload-to-cpu` by default so it coexists with the LLM on 12 GB; image button + `genImage()` in `chat.html`; tray auto-starts the bridge (`_start_all_services`) and kills it on exit via the Job Object; `/proxy/8086` route; `image/` + `*.safetensors` gitignored. All-local, no telemetry.
- **v0.826** — per-reply output download (first document-export step): each assistant reply gets `↓ .md` / `↓ .tsv` buttons under the bubble. `.md` saves the reply verbatim; `.tsv` extracts the first Markdown table as tab-separated with a UTF-8 BOM — tab dodges the Italian comma-decimal / semicolon-list CSV collision (a comma-CSV misparses on double-click in IT-locale Excel/Calc), the BOM keeps accented chars intact. Pure client-side (no new dependency, no bridge, nothing leaves localhost); `addDownloadBar()` called at the two live finalize sites. Manual-button trigger, not the LLM-decided tag protocol (deferred to avoid the `[SEARCH:]`/`QUERY:` parsing fragility); reloaded-from-history turns don't show the bar yet (deferred).
- **v0.824** — BUG-EXIT-01 fixed: `_do_exit` now calls `os._exit(0)` instead of `icon.stop()` — pystray's stop does not reliably unblock `icon.run()` on Windows, leaving the tray process hung after Exit. `selmo-models.ini` tuned: `[EuroLLM-22]` section added with `--cache-type-k/v q4_0` and `-ngl 54` to prevent KV-cache overflow on 12 GB VRAM; `[EuroLLM-9B]` ctx corrected to 4096 (native training context); `[Magistral]` ctx reduced 16384→8192 (same overflow fix, recovered ~15 t/s); `[Mistral]` kept as reference. GUI picker (`selmo_tray.py`) now shows the ini `note` inline next to each model name and uses a wider window. `chunk_pipeline.py`: fixed-size chunks (boundary detection removed); truncation detection via `finish_reason == length` now prefixes output with `[TRUNCATED]`; `available_tokens` capped at `max_tokens` to prevent 1:1 tasks (translation) from receiving chunks larger than the output budget.
- **v0.823** — no black window at startup: the tray now runs under `pythonw.exe` (via new `Selmo.vbs`, window style 0) so no console is ever allocated — hiding it was unreliable because on Windows 11 `GetConsoleWindow()` returns an internal helper window under Windows Terminal, not the visible tab. The console model picker is replaced by a Tkinter dialog (`_gui_picker`, still edits srv args + chunking size); `print()` diagnostics go to `selmo-tray.log`. `llama-server` launched with `CREATE_NO_WINDOW` so it doesn't pop its own console when the parent has none. Child cleanup no longer depends on the console ctrl handler: all children join a Windows Job Object with `KILL_ON_JOB_CLOSE`, so they die when the tray exits for any reason.
- **v0.820** — power without a kernel driver: CPU watts **estimated from load** (laptop/desktop profile via battery detection), GPU from NVML, LHM now **optional** (its WinRing0 driver is Defender-blocked and admin-only, so CPU power/temp don't read on a locked-down box); estimated figures flagged with `~`. Energy counter moved into the monitor as the single source of truth — `wh_session`/`wh_total` persisted in `selmo-wh.json` with `/reset_session` `/reset_total` endpoints — so multiple open UIs can't double-count (was client-side `localStorage`). Device line gained CPU load (temperature best-effort, LHM-only); session odometer reads to 0.1 Wh; hardware readouts regrouped under the gauge (watt split → VRAM/RAM → GPU/CPU load).
- **v0.818** — profile params mini-help: native `title` tooltips on Temp / Top-p / Top-k / System prompt in the profile modal, explaining each parameter and its use-case ranges on hover.
- **v0.817** — web search query-rewrite: a local RAG-style step (`rewriteQuery`) turns the chat turn into a keyword query before hitting the engine — fixes terse follow-ups (e.g. "17 anni" returning the movie) by inheriting the running topic from recent turns. Thinking disabled via `chat_template_kwargs.enable_thinking:false` (+ plain-call retry) since in-prompt `/no_think` is ignored by Qwen/Gemma; strict `QUERY:` line parsed from `content`/`reasoning_content`; the sources footer shows the query actually sent (`🔎 "…" ← original`). Graceful fallback to the raw message on any failure.
- **v0.816** — whole-system power: the main gauge now shows CPU+GPU+losses (system watts), not GPU-only. `selmo_gpu_monitor.py` reads CPU package + GPU power from LibreHardwareMonitor (`:8085/data.json`, vendor-agnostic — NVIDIA and AMD) and applies a PSU/baseline losses model. New `setup-lhm.ps1` makes it installer-reproducible (pinned download + config + elevated scheduled task, no manual GUI step); `selmo_server.py` launches LHM as a fallback.
- **v0.815** — three-profile system shipped (Selmo blue / Mizan red / Custom neutral): full Mizan red palette via R↔B channel swap + scoped surface overrides; Custom neutral palette with editable temp/top-p/top-k/system-prompt bound per-request; profile modal with three badges opened from the logo; profile persisted in `localStorage`. Mizan self-identifies; `/web` wording dropped from the system prompts; neutral welcome bubble. *(The profile system documented since s14 had never actually reached the committed `chat.html` — built from scratch this session; see Lessons.)*
- **v0.814** — docs streamline (consolidated Mizan notes, three-profile roadmap, `/web` → WEB toggle wording, mount-cache notes). Docs only — `chat.html` badge stayed v0.812.
- **v0.813** — `selmo-models.ini` chunk-ratio tuning.
- **v0.812** — consolidation checkpoint on the stable v0.811 base (recovery after a mount-cache regression made the working tree look reverted to v0.714; real disk was intact).
- **v0.811** — full parameter control: the ini carries the raw `srv=` server command; four structural flags prepended; `[Mistral]` section added.
- **v0.809** — models by folder (LM-Studio style, recursive scan, per-folder mmproj); vision output cap removed (was a flat 1200 that truncated answers).
- **v0.808** — VRAM/RAM gauges side by side; MoE offload `--n-cpu-moe` end-to-end; server status compacted to icon chips.
- **v0.807** — INI-driven launcher: per-model defaults out of the bat, first-match-wins sections.
- **v0.806** — THINK button re-activates when the model starts reasoning mid-stream.
- **v0.805** — reasoning panel preserved in loaded history (`renderStored` re-split) + model trace at top.
- **v0.804** — THINK auto-detected from the chat template; `INSTRUCTED`/`REASON_FIRST` flags; copy fix ("only web searches leave this machine").
- **v0.803** — THINK toggle locked (active + greyed) for reasoning-first models (Olmo).
- **v0.802** — reasoning panel for `<think>` tags and reasoning-first models; generalised flush state machine.
- **v0.801** — launcher runtime prompt for ngl/ctx; `Selmo.bat` translated to English.
- **v0.800** — VAD hands-free conversation (Silero `@ricky0123/vad-web`).
- **v0.716** �