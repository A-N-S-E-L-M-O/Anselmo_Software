# Modelli e binari enormi -- non versionare
models/
bin/
tts/
image/
*.dll
*.exe
*.zip
*.onnx
*.bin
*.gguf
*.safetensors

# Backup e file troncati
*.bak
*.back
*.truncated.bak
chat - Copy GOOD.html

# Log e temporanei
*.log
__pycache__/
*.pyc

# Lock
*.lock

# Certificati TLS del proxy HTTPS (rigenerati, contengono l'IP LAN)
selmo.crt
selmo.key
*.crt
*.key

# File di test pesanti
Test files/
chat.fab

# Runtime energy state (per-machine, written by selmo_gpu_monitor.py)
selmo-wh.json
selmo-cert-ip.txt

# Runtime image-model pick (per-machine: holds an absolute diffusion path,
# written by selmo_tray.py at startup; selmo-image-models.ini IS tracked)
selmo-image-config.json

# per-machine / shortcut (do not commit)
SelmoAI.lnk
selmo-state.json

# RAG mode: per-machine config (holds absolute corpus_dir) + built indexes
selmo-rag-config.json
selmo-rag.vecs.npy
selmo-rag.meta.json
selmo-rag.docs.vecs.npy
selmo-rag.docs.meta.json
selmo-rag.code.vecs.npy
selmo-rag.code.meta.json
